# LỘ TRÌNH PHÁT TRIỂN — HIỆN TẠI

## 1. Mục đích của tài liệu

Đây là **tài liệu trạng thái chính thức** của dự án website `kienthucdonghoco.vn` — nơi duy nhất phản ánh đúng những gì **đã hoàn thành**, **đang chờ** và **chưa thực hiện**.

Các tài liệu khác có ngày cũ (`docs/danh-gia-toan-dien-2026-08-28.md`, `docs/lo-trinh-con-lai-2026-08-28.md`, các đánh giá trước đó) **chỉ dùng để tham khảo lịch sử**. Khi một con số hoặc một nhận định trong tài liệu cũ khác với tài liệu này, **tài liệu này là chuẩn**.

Số liệu trong tài liệu được đếm trực tiếp từ mã nguồn tại thời điểm ghi ở phần 2 — không sao chép từ tài liệu cũ.

---

## 2. Mốc kiểm tra

- **Ngày thực hiện:** 10/09/2026 (cập nhật lần 22 — **đồng bộ tài liệu tiến độ cuối danh mục `DHC-REMAINING-20260906` (gói P1.5)**: đối chiếu Git sáu gói đã nghiệm thu và push (P0.3, P1.4, P2.1, P3.1, P3.2, P3.3 — bảng ở phần 6), đo lại toàn bộ số liệu bằng công cụ đếm có thể chạy lại (`output/p1.5-documentation-sync/`), sửa các trạng thái lỗi thời gồm câu "gói tourbillon chờ commit trên nền `12d01b3`" (đối chiếu Git: cụm tourbillon & điểm chuông song ngữ đã commit `20d0aa1`) và dòng "chưa có kho bài tiếng Anh" ở phần 5 (English launch bắt đầu ngày 02/09/2026; tại mốc đo 10/09/2026 có 64 route EN, gồm 53 bài và 11 trang khác). Lần 21 (04/09/2026): cụm "Hoàn thiện bộ máy: Guilloché, Perlage và Côtes de Genève" song ngữ; lần 20: cụm "Tourbillon & điểm chuông" song ngữ; lần 19: cụm "Lịch vạn niên & pha trăng" song ngữ; lần 18: cụm "Bộ điều chỉnh nhịp & bộ thoát" song ngữ; lần 17: cụm "Độ chính xác & chứng nhận" song ngữ; lần 16: cụm "Bảo vệ bộ máy" song ngữ; lần 15: cụm "Automatic & năng lượng cơ học" song ngữ; lần 14: cụm "Sử dụng an toàn hằng ngày" song ngữ; lần 13: cụm Chronograph & Tachymeter song ngữ; lần 12: cụm GMT song ngữ).
- **Commit nền của gói:** `1e4551b` — "docs(checks): conclude P3.3 audit with cluster scripts retained" (nhánh `main`, trùng `origin/main` khi bắt đầu gói; trước đó: `b2a4ba0` P3.2 giữ nguyên gradient, `f33b4f0` P3.1 giữ nguyên ảnh, `45e0fa6` P2.1 CSP preview, `82f450e` P1.4 nhận định tài chính, `c26973d` P0.3 infographic chronograph/lịch vạn niên). Ghi đối chiếu lịch sử: cụm "Tourbillon & điểm chuông" song ngữ (16 tệp, lần 20) từng ghi "đang chờ commit trên nền `12d01b3`" — đối chiếu Git 10/09/2026 cho thấy gói đó **đã commit `20d0aa1`** ("feat(i18n): add sourced high complications knowledge cluster") trong khoảng `12d01b3..1e4551b`; chi tiết thay đổi và kết quả kiểm tra được ghi tại biên bản nghiệm thu cụm hoàn thiện bộ máy (`docs/nghiem-thu/2026-09-04_nghiem-thu-cum-hoan-thien-bo-may-song-ngu.md`).
- **Nhánh hiện tại:** `main`.
- **Kết quả `npm run check` (trong chuỗi build 10/09/2026):** ĐẠT — chuỗi `check` trong package.json gồm **26 lệnh: 25 lệnh kiểm trực tiếp + `npm run check:types` (astro check) cuối chuỗi**. 25 lệnh kiểm theo thứ tự: quét ký tự (`scan-chars`), class trùng (`check-duplicate-classes`), text ẩn (`scan-hidden-text`), tương phản màu (`check-color-contrast` — 35/35 cặp token đạt AA), bàn phím (`check-keyboard-accessibility`), ngữ nghĩa (`check-semantic-accessibility`), chuyển động (`check-motion-accessibility`), nội dung (`check-content-accessibility`), dữ liệu sơ đồ tiến hóa (`check-evolution-data`), lộ trình học (`check-learning-paths`), và 15 kiểm cụm/nội dung: `check-first-watch-cluster`, `check-editorial-links`, `check-care-cluster`, `check-gmt-source-integrity`, `check-chronograph-cluster`, `check-daily-care-cluster`, `check-automatic-energy-cluster`, `check-protection-cluster`, `check-precision-certification-cluster`, `check-regulating-cluster`, `check-calendar-complications-cluster`, `check-high-complications-cluster`, `check-movement-finishing-cluster`, `check-brand-value-retention`, `check-financial-claims-cluster` — tất cả không có lỗi; `astro check`: **176 tệp, 0 errors / 0 warnings / 4 hints** (nguyên trạng 10/09/2026 — 4 hints ts(6133) biến không dùng trong tệp công cụ của các gói bằng chứng đã commit: `output/p3.1-image-audit/do-dinh-dang.mjs` (basename), `output/p3.2-svg-gradient-audit/phan-nhom.cjs` (sig) và `quet-gradient.cjs` (lines), `output/p3.3-cluster-script-audit/kiem-thu-hoi-quy.cjs` (duongDan); không phải 0/0/0). Chạy riêng sau build: **`scripts/check-english-launch.mjs`** (9 nhóm tiêu chí kiến trúc đa ngôn ngữ — ĐẠT, đủ 60 route bắt buộc trong `dist`).
- **Kết quả build 10/09/2026 (chạy lại thật trong gói P1.5, 22:27:24 → 22:29:36 +07, exit 0, 132 giây; lệnh `npm run build` chứa toàn bộ `npm run check` bên trong):** **286 tệp HTML do Astro build** — phân loại theo ngôn ngữ: **221 route tiếng Việt + 64 route `/en/` + 1 tệp `404.html`** (Astro tính trang 404 vào tổng build; `404.html` không phải route điều hướng công khai và không nằm trong sitemap — con số "222 tiếng Việt" ở các mốc trước là cách đếm gộp 404 vào tiếng Việt; hai cách đếm cùng dẫn tới 286). **19.827 liên kết nội bộ** (`scripts/check-links.mjs`: quét 286 trang HTML, 19.827 link, **0 hỏng** — tăng 5 so với mốc cũ 19.822 ngày 04/09/2026; chưa phân rã nguyên nhân theo từng gói). Pagefind index 286 trang. Kiểm ngân sách 3D ĐẠT (tổng chunk 3D 135,7 KB gzip — engine 131,4 KB + OrbitControls — chỉ tải khi người dùng chủ động mở tab "Mô hình 3D" trên `/giai-phau/`; 5 route kiểm không tải chunk 3D ban đầu). Kiểm route timeline song ngữ (`check-evolution-routes.mjs`) ĐẠT. Sitemap: `sitemap-index.xml` chỉ chứa **1 tham chiếu tới sitemap con** (không phải URL trang); `sitemap-0.xml` gồm **285 URL trang: 221 tiếng Việt + 64 `/en/`** — đếm bằng công cụ đếm của gói (tách index khỏi URL trang, có kiểm tra tổng = VI + EN). Số liệu là build cục bộ, chưa thay thế dữ liệu Core Web Vitals production.
- **Số liệu đếm từ mã nguồn (frontmatter, ngày 10/09/2026 — đo lại bằng `output/p1.5-documentation-sync/do-so-lieu.cjs`, khớp mốc 01/09/2026):** `relatedModels` mẫu ↔ mẫu **131 mục / 69 bài**; `relatedModels` từ bài cơ chế **29 mục / 18 bài**; `relatedMechanisms` trên bài iconic **29 mục / 24 bài** (chi tiết ở phần 5).
- **Nghiệm thu trình duyệt bốn công cụ tương tác:** ĐẠT trên bản preview sau build — biên bản tại `docs/nghiem-thu/2026-08-30_nghiem-thu-cong-cu-tuong-tac.md`.

**Lưu ý quan trọng:** các kiểm tra tự động chỉ rà được phần tĩnh của mã nguồn. Chúng **không chứng minh toàn bộ trải nghiệm trên website đã triển khai** — tốc độ thật trên máy người dùng, hoạt động trên điện thoại thật, màn hình thật, bàn phím thật, trình đọc màn hình thật vẫn phải nghiệm thu thủ công (xem phần 7 và phần 9).

---

## 3. Quy mô nội dung hiện tại

Số liệu đếm trực tiếp ngày 10/09/2026 bằng `output/p1.5-documentation-sync/do-so-lieu.cjs` (chạy lại được; kết quả máy-đọc `ket-qua-do.json`):

| Mảng | Bài tiếng Việt | Tệp tiếng Anh (`en/`) |
|---|---:|---:|
| Thương hiệu (`thuongHieu`) | **73** | 3 |
| Mẫu iconic (`mauIconic`) | **69** | 4 |
| Cơ chế (`coChe`) | **18** | 14 |
| Thuật ngữ (`tuDien`) | **33** | 23 |
| Hướng dẫn (`huongDan`) | **14** | 9 |
| Trang tĩnh (`trang`) | 1 (`ban-quyen` — trang pháp lý, không tính vào tổng bài) | 0 |
| **Tổng bài nội dung** | **207** (208 tệp nếu kể bài `/ban-quyen`) | **53** |

- **Cơ chế draft (xuất bản):** `draft: true` bị loại khỏi route ở bản production qua `src/lib/content.ts` ("bản nháp, không hiện trên web"). Tại thời điểm đếm **0 tệp nào có `draft: true`** ở mọi collection nên số tệp trên đĩa = số nội dung được xuất bản.
- **Quan hệ với số trang build:** 53 tệp English + trang chủ EN, 5 trang tĩnh EN và 5 trang danh sách EN = **64 route `/en/` trong `dist`** (đo được, khớp `check-english-launch` đủ 60 route bắt buộc).
- **Mốc lịch sử:** 28 mốc trong `src/data/timeline.json`, hiển thị trên trang `/lich-su`.
- **Hình minh họa lịch sử:** có **28 hình SVG minh họa cho 28 mốc** trong `public/images/timeline/` (mỗi mốc một SVG cùng slug). Trang `/lich-su` ưu tiên dùng ảnh JPG của mốc nếu tồn tại, nếu không sẽ dùng SVG cùng slug — hiện chưa có JPG tương ứng nên **28 SVG đang là minh họa hiển thị của 28 mốc**.

Nguồn tham khảo (đếm lại 10/09/2026): 73/73 thương hiệu, 69/69 mẫu iconic, 18/18 cơ chế, 33/33 thuật ngữ đều có khối nguồn; **14/14 bài hướng dẫn có nguồn** (4 bài cụm "bảo dưỡng & sử dụng an toàn" được bổ nguồn ngày 02/09/2026 — trước đó 10/14; hồ sơ tại `docs/ho-so-nguon-bao-duong-va-su-dung-an-toan.md`).

---

## 4. Những phần đã hoàn thành

Tóm tắt tình trạng tại mốc rà soát ngày 10/09/2026:

- **Nền tảng Astro và hệ thống nội dung** — website tĩnh Astro + Tailwind, nội dung Markdown theo bộ sưu tập có kiểm tra schema; xây bài mới không cần đụng mã nguồn.
- **Giao diện sáng/tối** — toàn trang có hai chế độ, màu quản bằng token (`--ig-*`, `--obs-*` cho hình vẽ; token Tailwind cho giao diện).
- **Tìm kiếm toàn website** — hộp tìm kiếm dùng chỉ mục Pagefind, sinh khi build.
- **So sánh** — trang `/so-sanh` đối chiếu các mẫu iconic.
- **SEO kỹ thuật cơ bản** — sitemap tự sinh khi build, dữ liệu có cấu trúc (structured data), ảnh chia sẻ mạng xã hội (OG) tự sinh.
- **WCAG 2.2 AA ở mức nền tảng** — bộ kiểm tra tự động (gồm `check-motion-accessibility` và phần WCAG trong `npm run check`) đạt; trang công bố `/kha-nang-tiep-can` khớp thực tế tại thời điểm rà. **Không phải "đạt WCAG toàn diện tuyệt đối"**: tương phản, reflow, zoom 200%, bàn phím, trình đọc màn hình trên **trình duyệt và thiết bị thật** vẫn cần nghiệm thu thủ công định kỳ.
- **Giải phẫu 2D/3D** — trang `/giai-phau` có sơ đồ tách lớp 2D và mô hình 3D xoay/zoom/tách lớp tải theo yêu cầu, có bản 2D dự phòng khi 3D không tải được.
- **Hệ thống infographic đã nâng cấp** — Tổng bộ được rà: **28 thành phần đồ họa/công cụ, đủ 28 đã qua đợt đồng bộ chất liệu**: 12 infographic cơ chế, 10 minh họa thuật ngữ, 2 bản giải phẫu (2D và 3D), và **4 công cụ tương tác — `BezelDiver`, `DateSafety`, `GmtReader`, `TachymeterTool` — đã hoàn tất nâng cấp và nghiệm thu nhóm ngày 30/08/2026** (biên bản: `docs/nghiem-thu/2026-08-30_nghiem-thu-cong-cu-tuong-tac.md`). **AntiMagnetic và Crystal đã được nâng cấp**. **`MechanismAnimation` là khung điều khiển dùng chung** (nút bấm, đếm bước, hỗ trợ giảm chuyển động) — **không phải một infographic độc lập còn thiếu**. Ghi chú trung thực: vẫn còn một số ít mã màu trực tiếp chưa chuyển hết sang token — 6 chỗ trong `Escapement`, 17 chỗ trong bản 2D giải phẫu (`WatchExplodedView`), và 4 điểm nhấn nhỏ (viền thẻ giải thích/bóng đổ JS) ở mỗi một trong bốn minh họa thuật ngữ làm trước (`Chronograph`, `GMT`, `Hairspring`, `Tourbillon`).
- **Dòng thời gian lịch sử** — trang `/lich-su` với 28 mốc từ `timeline.json`.
- **Bài hoàn thiện thủ công** — `hoan-thien-thu-cong-dong-ho` đã xuất bản (commit `f5e1ecf`): bài dài nhất trang, 7 mục, 6 nguồn chuyên ngành (Poinçon de Genève, AHCI, Grand Seiko, Breguet, SVMA, Anglage), liên kết nội bộ dày nhất trang.
- **Hạ tầng liên kết biên tập** — schema `relatedModels` / `relatedMechanisms`, khối hiển thị "Kết nối cùng chủ đề" trên trang mẫu iconic, thương hiệu và cơ chế đã hoạt động; **script kiểm tự động `scripts/check-editorial-links.mjs` đã chạy trong `npm run check`** (kiểm slug hỏng, trùng lặp, thiếu relation và bắt buộc hai chiều cho quan hệ cơ chế ↔ mẫu iconic); tài liệu đề xuất 15 nhóm quan hệ tại `docs/de-xuat-lien-ket-bien-tap-v1.md` đã áp phần lớn (chi tiết ở phần 5).

---

## 5. Những phần đã có hạ tầng nhưng chưa tạo giá trị đầy đủ

- **Liên kết biên tập (`relatedModels` / `relatedMechanisms`):** mạng liên kết giữa các mẫu iconic **đã hoàn tất ở phạm vi hiện tại** — đếm lại từ frontmatter ngày 10/09/2026 (khớp mốc 01/09/2026): **69/69 bài mẫu iconic** có dữ liệu `relatedModels` với tổng **131 mục liên kết** (giữa các mẫu trong cùng cụm/tuyến chủ đề). **Chiều cơ chế ↔ mẫu iconic: 29 cặp quan hệ hai chiều** (script `check-editorial-links.mjs` đếm) — gồm `relatedModels` từ bài cơ chế **29 mục trong 18 bài** và `relatedMechanisms` trên **29 mục trong 24 bài iconic**; **không còn bài cơ chế nào thiếu quan hệ**: bốn cơ chế còn trống trước đó đã được nối ở gói này — `gmt` ⇄ `rolex-gmt-master`, `perpetual-calendar` ⇄ `audemars-piguet-royal-oak-perpetual-calendar` và `patek-philippe-grandmaster-chime`, `diem-chuong` ⇄ `patek-philippe-grandmaster-chime`, `bo-thoat` ⇄ `freak` (chi tiết ở biên bản `docs/nghiem-thu/2026-09-01_nghiem-thu-ba-mau-iconic-complication.md`). **Các trang thương hiệu tự động lấy mẫu iconic tương ứng qua việc đối chiếu tên thương hiệu** (khối "Mẫu iconic của…" trên trang hãng) — **không cần thêm thủ công `relatedModels` vào 73 bài thương hiệu**. Gói thương hiệu → mẫu iconic (thêm `relatedModels` vào 66 trang hãng) đã dừng vì cần mở hạ tầng trước (schema `thuongHieu` hiện chưa có trường này; template thương hiệu chưa render nó) — chờ quyết định.
- **Analytics:** đã tích hợp (Vercel Web Analytics) nhưng chưa xem xét số liệu thực tế — chưa có đánh giá hành vi người đọc dựa trên dữ liệu.
- **Giao diện đa ngôn ngữ:** English launch bắt đầu ngày 02/09/2026; tại mốc đo 10/09/2026 có **64 route EN** (`/en/`), gồm **53 bài** và **11 trang khác** (trang chủ, 5 trang tĩnh, 5 trang danh sách), bảng địa chỉ trung tâm `src/i18n/contentRoutes.ts`; các cụm đã song ngữ liệt kê ở phần 7. Giới hạn hiện tại: khối nội dung tiếng Việt chưa dịch **chỉ tiếng Việt và được ẩn khỏi English** (trạng thái hợp lệ); chưa có người biên tập tiếng Anh chuyên trách và chưa có dữ liệu Search Console cho bản English.
- **Newsletter:** mới ở trạng thái **chuẩn bị** — chỉ có trong kế hoạch (Giai đoạn 4, "Newsletter hoạt động thật"); chưa có gì trong mã nguồn và chưa hoạt động thực tế. Form liên hệ hiện dùng Formspree gói miễn phí (giới hạn 50 lượt/tháng — ghi ở `CAN-KIEM-CHUNG.md` mục 24).

---

## 6. Danh mục `DHC-REMAINING-20260906` — trạng thái và giới hạn

Danh mục gồm 7 gói (P0.3, P1.4, P2.1, P3.1, P3.2, P3.3, P1.5). Trạng thái đối chiếu Git ngày 10/09/2026 — cả sáu gói đầu **đã nghiệm thu và push lên `main`** (hash xác minh bằng `git merge-base --is-ancestor`):

| Gói | Commit | Kết quả đã nghiệm thu |
| --- | --- | --- |
| P0.3 | `c26973d` | Làm sạch infographic chronograph & lịch vạn niên + hàng rào kiểm tương ứng |
| P1.4 | `82f450e` | Xử lý nhận định tài chính theo phạm vi đã duyệt + script chống hồi quy `check-financial-claims-cluster` |
| P2.1 | `45e0fa6` | Kiểm chứng CSP trên preview cách ly (2 bản deploy); **không triển khai enforcement production** |
| P3.1 | `f33b4f0` | Đánh giá định dạng ảnh: **giữ nguyên toàn bộ ảnh**, không mở chặng chuyển định dạng |
| P3.2 | `b2a4ba0` | Đánh giá gradient SVG: **giữ nguyên gradient**, không tái cấu trúc |
| P3.3 | `1e4551b` | Đánh giá script kiểm cụm: **giữ nguyên 13 script, không tách helper** |
| P1.5 | Commit chứa bản cập nhật này | **GPT Work đã nghiệm thu ĐẠT.** Hoàn tất đồng bộ tài liệu; giao dịch đóng danh mục có hiệu lực khi commit này được xác minh trên `origin/main`. Không tự ghi hash của chính commit vào nội dung |

**Kết luận đóng danh mục:** bảy gói đã được nghiệm thu; sau khi commit P1.5 được push và xác minh, danh mục `DHC-REMAINING-20260906 v1.0.0` hoàn tất. Dừng triển khai theo danh mục này. Những giới hạn và backlog ngoài danh mục bên dưới vẫn giữ nguyên, không được coi là đã xử lý hoặc tự động được duyệt triển khai.

**Giới hạn đã ghi nhận của các gói (được chấp thuận — không mở lại trong danh mục này):**

- **P2.1:** enforcement CSP + `'wasm-unsafe-eval'` chỉ được thử trên **preview cách ly**; production giữ Report-Only — đó là trạng thái tại thời điểm đối chứng đã lưu (10/09/2026), không phải kết quả kiểm tra live mới. Beacon analytics (`vitals.vercel-insights.com`) **chưa quan sát được request**; hành vi Escape khi tìm kiếm (xóa chữ nhưng panel kết quả chưa ẩn) **chưa xác định có sẵn từ trước** — cả hai là giới hạn bằng chứng được chấp nhận, không chứng minh analytics hoạt động đầy đủ.
- **P3.1:** giữ nguyên cả hai tệp chưa tìm thấy tham chiếu (`hero-bg.jpg`, `guilloche-tile.png` — 493.486 byte) — **chưa được xóa hay tối ưu**, chỉ ghi nhận; kết luận "không tìm thấy tham chiếu" chỉ trong phạm vi đã quét.
- **P3.2:** bốn nhóm ID pattern `guilloche` trùng toàn tài liệu (`/lich-su/` 28 lần + 3 trang thương hiệu mỗi trang 2 lần — tổng 34 lần, 30 phần tử dư); **tác động hiển thị chưa được xác minh**; giữ nguyên tệp liên quan.
- **Reduced motion của hoạt ảnh Escapement:** còn giới hạn kiểm chứng (JS không đọc `prefers-reduced-motion`, chỉ CSS `.step-bar`) — **không tuyên bố accessibility toàn website đạt**.
- **P3.3:** bộ 35 ca hồi quy + 4 phép thử tự kiểm driver là **đường cơ sở giới hạn** — không phủ mọi rule của 13 script; **không có độ phủ tương đương bộ 37 cũ** (bộ 37 cũ gồm 2 ca component tách nhóm riêng, đã gộp/bỏ khi rà thiết kế — biên bản P3.3 mục 5.1).
- **Diagnostics hiện hành** (astro check 176 tệp, 10/09/2026): 0 errors / 0 warnings / **4 hints** (biến không dùng trong 4 tệp công cụ của gói bằng chứng đã commit — liệt kê ở phần 2).

Biên bản nghiệm thu từng gói: `docs/nghiem-thu/P2.1-csp-preview-2026-09-09.md`, `docs/nghiem-thu/P3.1-danh-gia-dinh-dang-anh-2026-09-10.md`, `docs/nghiem-thu/P3.2-danh-gia-gradient-svg-2026-09-10.md`, `docs/nghiem-thu/P3.3-danh-gia-script-kiem-cum-2026-09-10.md` (P0.3 và P1.4 có hồ sơ trong lịch sử commit tương ứng).

---

## 7. Những việc còn lại theo thứ tự

### Ưu tiên 0

- **Hoàn thiện hồ sơ trạng thái và làm sạch tracker kiểm chứng** — chính là gói việc này: `docs/LO-TRINH-PHAT-TRIEN-HIEN-TAI.md` trở thành tài liệu trạng thái duy nhất; `CAN-KIEM-CHUNG.md` được rà lại và thống nhất cách ghi trạng thái; `PROJECT.md` gắn ghi chú rõ là bản kiến trúc khởi đầu, không dùng để tra tiến độ.

### Ưu tiên 1

- **Kiểm chứng và áp dụng liên kết biên tập** — duyệt 15 nhóm đề xuất, điền dữ liệu vào frontmatter theo đợt nhỏ, kiểm tra hiển thị cả sáng/tối và mobile. **(Hoàn tất ở phạm vi hiện tại cho chiều mẫu ↔ mẫu: 69/69 bài iconic có `relatedModels` — 131 mục. Chiều cơ chế ↔ mẫu: 29 cặp hai chiều theo các cụm chronograph, tourbillon, pha trăng, lên dây tự động, dạ quang, chống nước, trữ cót, chống từ, chuỗi truyền động, dây tóc, chống sốc, hiển thị ngày, kính, GMT, lịch vạn niên, điểm chuông và bộ thoát–silic; mọi bài cơ chế đều có ít nhất một quan hệ. Script kiểm tự động hai chiều đã vào `npm run check`.)**
- **Chuẩn bị dữ liệu reference có nguồn** — cho sơ đồ tiến hóa dòng sản phẩm: chốt dòng nào làm trước, thu thập mốc năm/reference có kiểm chứng; nguyên tắc "thà thiếu còn hơn sai", ghi mục chưa chắc vào `CAN-KIEM-CHUNG.md`. **(Đã có hồ sơ cho 2 dòng: `docs/ho-so-du-lieu-tien-hoa-rolex-submariner.md` ngày rà 30/08/2026 — 8 mốc đủ nguồn, 5 điểm còn treo ở bảng "Cần kiểm chứng"; và `docs/ho-so-du-lieu-tien-hoa-rolex-gmt-master.md` ngày rà 01/09/2026 — 8 mốc đủ nguồn, mốc bị loại có lý do riêng. Nhân rộng sang dòng mới bắt buộc phải có hồ sơ nguồn riêng trước.)**
- **Làm thử một sơ đồ tiến hóa reference** — thí điểm một dòng (một sơ đồ khái niệm, ghi rõ là khái niệm nếu không chắc tỷ lệ/mốc), đánh giá rồi mới nhân rộng. **(Thí điểm 30/08/2026 với Rolex Submariner — 8 mốc đủ nguồn tại `https://www.kienthucdonghoco.vn/mau-iconic/rolex-submariner/`, biên bản `docs/nghiem-thu/2026-08-30_nghiem-thu-so-do-tien-hoa-submariner.md`; ngày 01/09/2026 đã chuẩn hóa thành hạ tầng dùng chung: `src/data/modelEvolution.ts` (kiểu + sổ đăng ký + điều kiện render), component tổng quát `src/components/ModelEvolution.astro`, dataset riêng theo dòng (`submarinerEvolution.ts`, `rolexGmtMasterEvolution.ts`) và script kiểm dữ liệu `scripts/check-evolution-data.mjs` đã vào `npm run check`. **Đã xuất bản 2 sơ đồ tiến hóa** — Submariner (8 mốc, dữ kiện giữ nguyên từ thí điểm) và GMT-Master (8 mốc, `https://www.kienthucdonghoco.vn/mau-iconic/rolex-gmt-master/`). Dữ liệu vẫn có giới hạn: mỗi sơ đồ chỉ gồm các mốc đã kiểm chứng trong hồ sơ riêng của dòng — không có hồ sơ nguồn thì chưa làm sơ đồ. Biên bản `docs/nghiem-thu/2026-09-01_nghiem-thu-so-do-tien-hoa-gmt-master.md`.) **Ngày 02/09/2026 (Prompt 31): sơ đồ Submariner thành song ngữ vi+en** — dataset chuyển sang `LocalizedText {vi,en}` + `publishedLangs` (Submariner `['vi','en']`, GMT-Master `['vi']` — chưa render English); component `ModelEvolution` nhận `lang`, không còn chữ hard-code; kiểm mở rộng trong `check-evolution-data.mjs` (tiêu chí song ngữ 1–8) + `scripts/check-evolution-routes.mjs` vào `npm run build`; biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-timeline-submariner-song-ngu.md`.)** **Ngày 02/09/2026 (Prompt 32): GMT-Master hoàn chỉnh song ngữ** — bài English thật `/en/iconic-watches/rolex-gmt-master/` (cùng dữ kiện + 2 nguồn với bản vi), cặp route thêm vào `contentRoutes.ts`, dataset GMT chuyển `LocalizedText` + `publishedLangs ['vi','en']` — timeline hiện ở cả hai route; `check-evolution-routes.mjs` viết lại generic theo `publishedLangs` của mọi dataset; biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-english-gmt-master.md`.)**
- **Trang hub "Lộ trình học đồng hồ cơ"** — bản đồ đường đọc cho ba nhóm người đọc (bắt đầu chơi, hiểu bộ máy, tư duy người sưu tầm), chỉ **tổ chức lại các bài/trang đã tồn tại** — không tạo dữ kiện hay bài mới. **(Hoàn tất 01/09/2026: trang `/lo-trinh-hoc-dong-ho/` gồm 3 lộ trình — 5 + 6 + 6 bước, mỗi bước là một liên kết thật kèm lý do đọc; dữ liệu tập trung ở `src/data/learningPaths.ts`; script kiểm `scripts/check-learning-paths.mjs` đã vào `npm run check` (đủ 3 lộ trình, ≥5 bước/lộ trình, route nội bộ tồn tại, không URL ngoài); điểm vào từ nhóm menu "Kiến thức" (desktop + mobile) và CTA trên trang chủ; hiển thị đầy đủ khi không có JavaScript. Giới hạn: giá trị nằm ở tổ chức đường đọc — chưa phải kết quả SEO hay Analytics thực tế. Biên bản `docs/nghiem-thu/2026-09-01_nghiem-thu-lo-trinh-hoc-dong-ho.md`.)**
- **Tối ưu hiệu năng phần 3D giải phẫu + ngân sách tải** — Three.js chỉ tải khi người dùng chủ động mở tab "Mô hình 3D"; engine dùng named imports (không namespace), OrbitControls tách chunk cache riêng; script chống hồi quy `scripts/check-3d-loading-budget.mjs` chạy trong `npm run build` sau Astro build — xác nhận 5 route kiểm (trang chủ, thương hiệu, mẫu iconic, cơ chế, `/giai-phau/` ở 2D mặc định) không tải chunk 3D ban đầu. **(Hoàn tất 01/09/2026; chi tiết số liệu trước/sau và nghiệm thu trình duyệt ở biên bản `docs/nghiem-thu/2026-09-01_nghiem-thu-hieu-nang-giai-phau-3d.md`. Giới hạn: kích thước chunk 3D là bản chất của Three.js; chưa có dữ liệu Core Web Vitals production.)**
- **Nâng cấp cụm nội dung "Chọn đồng hồ cơ đầu tiên"** — bài trụ cột viết lại thành **khung quyết định cho người mới** theo hướng people-first: bỏ bảng giá/ngân sách, tư vấn đầu tư/"giữ giá", chu kỳ bảo dưỡng cố định và bảng kích thước tuyệt đối (không có nguồn); thêm **Bàn quyết định** (DecisionMap — bản đồ điều hướng 3 nhánh đầu bài, HTML tĩnh không script) và liên kết hai chiều với 5 bài hỗ trợ + Lộ trình học + Từ điển; kiểm cụm tự động `scripts/check-first-watch-cluster.mjs` đã vào `npm run check`. **(Hoàn tất 02/09/2026; hồ sơ nguồn tại `docs/ho-so-nguon-chon-dong-ho-co-dau-tien.md`, chiến lược cụm — ghi rõ là giả định cần kiểm chứng bằng Search Console — tại `docs/chien-luoc-seo-cum-chon-dong-ho-dau-tien.md`, biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-cum-seo-chon-dong-ho-dau-tien.md`. Đây là cụm nội dung people-first, không phải cam kết thứ hạng SEO; chưa có dữ liệu Search Console/Analytics để kết luận hiệu quả.)**
- **English launch pack — bản tiếng Anh thật cho người đọc quốc tế** — English launch bắt đầu ngày 02/09/2026 (27 route EN tại ngày ra mắt — biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-english-launch-pack.md`); tại mốc đo 10/09/2026 có 64 route EN, gồm 53 bài và 11 trang khác. Kiến trúc: `astro.config` locales `['vi','en']` (vi mặc định, không tiền tố; en bắt đầu `/en/`); bảng địa chỉ trung tâm `src/i18n/contentRoutes.ts` (**63 cặp vi↔en** — nguồn duy nhất cho switcher, hreflang, footer, link trong template); `getSlug` ưu tiên `custom_slug` (không file vi nào có — URL vi nguyên vẹn); 5 template bài dùng chung (`src/components/templates/`) + wrapper mỏng cho route vi và en; **64 route `/en/` (mốc đo 10/09/2026)**: trang chủ, 5 trang tĩnh (about/accessibility/contact/copyright/learning-path), 5 trang danh sách, 53 bài nền tảng (9 guide, 14 cơ chế, 23 thuật ngữ — gồm 1 trang tương thích `/en/glossary/escapement/` giữ cho liên kết đã công bố, trỏ về `/en/glossary/escape-wheel/`, route hiện hành của cặp Việt–Anh là escape-wheel — Rolex/Omega/Seiko, Submariner/Speedmaster/Tank/GMT-Master) cùng dữ kiện + nguồn với bản vi, thuật ngữ horology chuẩn. **Cụm GMT song ngữ** (cơ chế `gmt`, thuật ngữ `gmt`, hướng dẫn `reading-and-setting-gmt`), **cụm Chronograph & Tachymeter song ngữ** (cơ chế `chronograph`, thuật ngữ `chronograph` + `tachymeter`, hướng dẫn `using-a-tachymeter`) và **cụm "Sử dụng an toàn hằng ngày" song ngữ** (hướng dẫn lên dây/chống nước/chỉnh lịch, cơ chế chống nước, thuật ngữ núm vặn + dây cót) và **cụm "Automatic & năng lượng cơ học" song ngữ** (cơ chế `/en/mechanisms/automatic-winding/`, thuật ngữ `/en/glossary/barrel/`, hướng dẫn `/en/guides/watch-winders/` — kèm chuẩn hóa 6 bài Việt về automatic/rotor/thùng cót/trữ cót/hộp xoay và rà 3 bài English có sẵn theo nguồn FHH) và **cụm "Bảo vệ bộ máy: chống từ, chống sốc & Incabloc" song ngữ** (cơ chế `/en/mechanisms/anti-magnetism/` + `/en/mechanisms/shock-protection/`, thuật ngữ `/en/glossary/incabloc/` — kèm chuẩn hóa 3 bài Việt về chống từ/chống sốc/Incabloc theo nguồn METAS, Oris, Incabloc SA và FHH) và **cụm "Độ chính xác & chứng nhận" (COSC / Master Chronometer) song ngữ** (hướng dẫn `/en/guides/accuracy-tracking/`, thuật ngữ `/en/glossary/chronometer/` + `/en/glossary/master-chronometer/` — kèm chuẩn hóa 3 bài Việt về theo dõi độ chính xác/chronometer/Master Chronometer theo nguồn COSC và METAS) và **cụm "Bộ điều chỉnh nhịp & bộ thoát" song ngữ** (cơ chế `/en/mechanisms/balance-and-hairspring/`, thuật ngữ `/en/glossary/escape-wheel/` + `/en/glossary/beat-rate/` — kèm chuẩn hóa 5 bài Việt về bộ thoát/dây tóc-bánh lắc/bánh thoát/VPH theo 5 mục FHH trực tiếp; thay cặp sai `/tu-dien/banh-thoat` → `/en/glossary/escape-wheel/`; giữ `/en/glossary/escapement/` làm trang tương thích tránh 404 cho liên kết đã công bố trước đây) và **cụm "Lịch vạn niên & pha trăng" song ngữ** (cơ chế `/en/mechanisms/perpetual-calendar/` + `/en/mechanisms/moon-phase/`, thuật ngữ `/en/glossary/perpetual-calendar/` — kèm chuẩn hóa 3 bài Việt về perpetual calendar/pha trăng theo 4 mục FHH trực tiếp) và **cụm "Tourbillon & điểm chuông" song ngữ** (cơ chế `/en/mechanisms/tourbillon/` + `/en/mechanisms/minute-repeater/`, thuật ngữ `/en/glossary/tourbillon/` + `/en/glossary/minute-repeater/` — kèm chuẩn hóa 4 bài Việt về tourbillon/điểm chuông theo 3 mục FHH trực tiếp) và **cụm "Hoàn thiện bộ máy: Guilloché, Perlage và Côtes de Genève" song ngữ** (hướng dẫn `/en/guides/movement-finishing/` cho cặp `/huong-dan/hoan-thien-thu-cong-dong-ho`, thuật ngữ `/en/glossary/guilloche/` + `/en/glossary/perlage/` + `/en/glossary/geneva-stripes/` — kèm chuẩn hóa 4 bài Việt theo nguồn FHH trực tiếp và quy định công bố của Poinçon de Genève; bài hướng dẫn đổi trọng tâm từ "nhận biết chất lượng hoàn thiện" sang "ba thuật ngữ mô tả loại xử lý bề mặt nào và vì sao không suy từ thuật ngữ/ảnh ra chất lượng vận hành, nguồn gốc, giá trị") xuất bản sau khi kiểm chứng và chuẩn hóa dữ kiện theo nguồn — hồ sơ `docs/ho-so-nguon-cum-gmt-song-ngu.md`, `docs/ho-so-nguon-cum-chronograph-tachymeter-song-ngu.md`, `docs/ho-so-nguon-cum-su-dung-an-toan-song-ngu.md`, `docs/ho-so-nguon-cum-automatic-nang-luong-song-ngu.md`, `docs/ho-so-nguon-cum-bao-ve-bo-may-song-ngu.md`, `docs/ho-so-nguon-cum-do-chinh-xac-chung-nhan-song-ngu.md`, `docs/ho-so-nguon-cum-bo-dieu-chinh-nhip-song-ngu.md`, `docs/ho-so-nguon-cum-lich-van-nien-va-pha-trang-song-ngu.md`, `docs/ho-so-nguon-cum-tourbillon-va-diem-chuong-song-ngu.md`, `docs/ho-so-nguon-cum-hoan-thien-bo-may-song-ngu.md`; autolink từ điển trên trang EN chỉ bọc thuật ngữ đã có cặp EN (lấy từ bảng contentRoutes), thuật ngữ chưa dịch giữ nguyên văn thay vì link về route tiếng Việt. Bài EN không nhắc công cụ tương tác/infographic (chỉ render tiếng Việt) — văn bản EN viết tĩnh. Bộ chuyển ngôn ngữ là link thật ở Header (desktop + mobile): trang chưa dịch → về trang chủ đích kèm aria-label giải thích. Pagefind lọc kết quả theo `language` của trang. SEO: `og:locale`/`inLanguage` động, hreflang vi+en+x-default chỉ khi cặp tồn tại, sitemap gồm 64 URL `/en/`, không redirect theo ngôn ngữ. Phần tương tác nhãn tiếng Việt (infographic, DecisionMap, công cụ cầm tay) ẩn ở bản EN thay vì hiện nửa dịch; **hai timeline tiến hóa đã song ngữ: Rolex Submariner và Rolex GMT-Master** — hiển thị trên cả hai route; timeline khác chỉ hiện English khi dataset của nó có `publishedLangs` chứa `en` (chưa dịch thì ẩn — trạng thái hợp lệ, không phải lỗi). History/Anatomy/Compare đặt ngoài menu English giai đoạn này. Kiểm tự động `scripts/check-english-launch.mjs` (9 nhóm tiêu chí, đủ 60 route bắt buộc trong `dist`, chạy sau build), kiểm chống hồi quy cụm `scripts/check-chronograph-cluster.mjs`, `scripts/check-daily-care-cluster.mjs`, `scripts/check-automatic-energy-cluster.mjs`, `scripts/check-protection-cluster.mjs`, `scripts/check-precision-certification-cluster.mjs`, `scripts/check-regulating-cluster.mjs` và `scripts/check-calendar-complications-cluster.mjs`, `scripts/check-high-complications-cluster.mjs` và `scripts/check-movement-finishing-cluster.mjs` (trong `npm run check`). **(Hoàn tất 02/09/2026; cụm GMT 03/09/2026, cụm Chronograph & Tachymeter 03/09/2026, cụm Sử dụng an toàn 03/09/2026, cụm Automatic & năng lượng cơ học 04/09/2026, cụm Bảo vệ bộ máy 04/09/2026, cụm Độ chính xác & chứng nhận 04/09/2026, cụm Bộ điều chỉnh nhịp & bộ thoát 04/09/2026, cụm Lịch vạn niên & pha trăng 04/09/2026, cụm Tourbillon & điểm chuông 04/09/2026, cụm Hoàn thiện bộ máy 04/09/2026 — biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-english-launch-pack.md`, `docs/nghiem-thu/2026-09-03_nghiem-thu-cum-gmt-song-ngu.md`, `docs/nghiem-thu/2026-09-03_nghiem-thu-cum-chronograph-tachymeter-song-ngu.md`, `docs/nghiem-thu/2026-09-03_nghiem-thu-cum-su-dung-an-toan-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-automatic-nang-luong-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-bao-ve-bo-may-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-do-chinh-xac-chung-nhan-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-bo-dieu-chinh-nhip-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-lich-van-nien-va-pha-trang-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-tourbillon-va-diem-chuong-song-ngu.md`, `docs/nghiem-thu/2026-09-04_nghiem-thu-cum-hoan-thien-bo-may-song-ngu.md`. Giới hạn trung thực: đây là launch pack nền tảng — toàn bộ khối nội dung chưa dịch tiếp tục chỉ tiếng Việt và được giấu khỏi English, không tạo trải nghiệm rỗng; chưa có dữ liệu Search Console. Cụm "Automatic & năng lượng cơ học", cụm "Bảo vệ bộ máy: chống từ, chống sốc & Incabloc", cụm "Độ chính xác & chứng nhận (COSC / Master Chronometer)", cụm "Bộ điều chỉnh nhịp & bộ thoát", cụm "Lịch vạn niên & pha trăng", cụm "Tourbillon & điểm chuông" và cụm "Hoàn thiện bộ máy: Guilloché, Perlage và Côtes de Genève" là cụm nội dung theo nguồn — không phải cam kết hiệu quả SEO hay dữ liệu người dùng thực tế.)**
- **Chuẩn hóa cụm "Bảo dưỡng & sử dụng an toàn" theo nguồn** — bài `bao-duong-dong-ho` viết lại toàn bộ theo 7 mục (service là gì / khi nào liên hệ chuyên môn / chuẩn bị thông tin / đọc báo giá / bảo quản giữa các lần / việc không nên tự làm / checklist trước khi giao), **bỏ toàn bộ con số không nguồn** (chu kỳ 3–4/4–5 năm, bảng chi phí theo hãng, "service mất X tuần", sai số tuyệt đối, "gấp X lần", hứa giá trị bán lại) và **các khái quát vượt nguồn** ("các hãng khá giống nhau", "service chính hãng thường bao gồm gioăng", chỉ dẫn thao tác "đến khi cảm nhận độ căng"), thay bằng dấu hiệu quan sát + câu hỏi để hỏi, OMEGA/Rolex chỉ là ví dụ quy trình riêng từng hãng; **`chon-co-dong-ho` viết lại hoàn toàn, loại bảng tra cổ tay → đường kính/vấu cùng mọi ngưỡng số đo và nhận định "cổ tay người Việt/Á"** — thay bằng khung đọc thông số của đúng mẫu + phương pháp đối chiếu thực tế + checklist mua online (nguồn Longines/TAG Heuer chỉ nâng ý "đo cổ tay và tham khảo thông số"); **`hop-xoay-dong-ho` viết lại thành khung quyết định trung tính** ("xác định gì trước khi dùng" — không claim lợi ích/cách cài/ảnh hưởng bảo dưỡng, không tự đặt TPD/chiều quay; nguồn FHH chỉ nâng cơ chế automatic/thùng cót); **`nhan-biet-dong-ho-gia` viết lại thành "giới hạn của tự kiểm"** (bỏ toàn bộ mục dấu hiệu bằng mắt — kim giây, tiếng máy, trọng lượng, hoàn thiện, giá; hộp/thẻ/serial không tự chứng minh; FH là nguồn thông tin + phản ánh, không phải dịch vụ xác thực). Thêm **Bàn quyết định cho bài bảo dưỡng** (heading "Bạn đang gặp tình huống nào?", 3 nhánh theo tình huống, 7 link) tái dùng hạ tầng DecisionMap; đổi 4 nhãn trong 2 map cho khớp nội dung bài mới. Kiểm cụm tự động `scripts/check-care-cluster.mjs` (pattern cấm riêng cho từng bài) đã vào `npm run check`. 4 bài đều có `updated: 2026-09-02` + khối nguồn HTTPS (tổng 10 nguồn, xác minh 02/09/2026). **(Hoàn tất 02/09/2026 sau hai vòng — vòng 2 loại các khẳng định "mềm" vẫn vượt nguồn; hồ sơ nguồn `docs/ho-so-nguon-bao-duong-va-su-dung-an-toan.md` ghi rõ từng nguồn nâng đỡ điều gì và KHÔNG nâng đỡ điều gì, chiến lược — giả định cần kiểm chứng — `docs/chien-luoc-seo-cum-bao-duong-va-su-dung-an-toan.md`, biên bản `docs/nghiem-thu/2026-09-02_nghiem-thu-cum-bao-duong-va-su-dung-an-toan.md`. Giới hạn: nguồn Rolex xác minh qua trình đọc trang do trang chặn truy cập tự động; chưa có dữ liệu Search Console.)**

### Ưu tiên 2

- **Xử lý các thông tin kiểm chứng có ảnh hưởng tới nội dung đang hiển thị** — **(Cập nhật 01/09/2026: ba liên kết nguồn chết Patek Philippe, COSC và Kurono Tokyo đã được thay bằng URL chính hãng hoạt động trong đủ bảy bài — 6 bài ban đầu cộng bài `patek-nautilus` được phát hiện khi rà toàn bộ `src/content/`; tracker `CAN-KIEM-CHUNG.md` không còn cờ ưu tiên đang mở cho ba URL này — xem bảng lịch sử xử lý ở bảng tổng hợp tracker.)** Các việc tiếp theo của nhóm này vẫn là:
  - **Nghiệm thu production trên mobile, desktop, sáng/tối, zoom 200% và bàn phím** — kiểm tra thủ công trên bản đã triển khai thật, không chỉ bản xem trước trên máy (chưa có bằng chứng thực hiện — vẫn treo).
  - **Theo dõi Search Console, Analytics và Core Web Vitals** — lập nhịp rà định kỳ khi có số liệu thực (chưa thực hiện — vẫn treo).
  - **Phát triển các cụm nội dung SEO** — nhóm bài chủ đề quanh từ khóa thực tế (chưa thực hiện — vẫn treo).

### Ưu tiên 3, có điều kiện

- **Nội dung tiếng Anh** — chỉ khi khối tiếng Việt đã ổn định và có người chịu trách nhiệm biên tập tiếng Anh.
- **Newsletter** — khi có công cụ gửi và nội dung định kỳ thực sự.
- **Các cải tiến trang trí không ảnh hưởng khả năng sử dụng** — làm sau khi các ưu tiên trên xong, không được làm giảm khả năng tiếp cận.

---

## 8. Những việc không nên làm lúc này

- **Không thiết kế lại toàn website** — nền giao diện và hệ thống token vừa đồng bộ xong, chưa có lý do đủ lớn.
- **Không thêm hiệu ứng chỉ để trang trí** — mọi chuyển động phải phục vụ việc giải thích và tôn trọng `prefers-reduced-motion`.
- **Không bổ sung hàng loạt thương hiệu chỉ để tăng số lượng** — chất lượng và nguồn của bài hiện có quan trọng hơn số lượng.
- **Không dịch toàn bộ website sang tiếng Anh** — khối tiếng Việt mới là nội dung đang phục vụ người đọc thật.
- **Không tự điền thông tin lịch sử chưa có nguồn** — năm, reference, calibre chỉ điền sau khi kiểm chứng; ghi vào `CAN-KIEM-CHUNG.md` khi chưa chắc.
- **Không cố xử lý mọi mục "chờ nguồn" như thể đó là lỗi đang xuất bản** — phần lớn mục trong `CAN-KIEM-CHUNG.md` đã được xử lý an toàn (đã ẩn hoặc đã diễn đạt lại), không gây sai cho người đọc; chỉ nhóm `CẦN ƯU TIÊN KIỂM CHỨNG` mới cần hành động sớm.

---

## 9. Điều kiện nghiệm thu chung

Mọi gói phát triển sau này phải:

1. Không làm hỏng URL hiện tại.
2. Không tạo liên kết chết.
3. Hoạt động ở cả chế độ sáng và tối.
4. Dùng được trên điện thoại.
5. Không làm giảm khả năng thao tác bằng bàn phím.
6. Tôn trọng `prefers-reduced-motion`.
7. Không làm giảm điểm tương phản.
8. `npm run check` đạt.
9. Nghiệm thu thủ công phần tương tác đã sửa (trên trình duyệt thật, cả hai chế độ sáng/tối).
10. Chỉ commit đúng tệp thuộc gói công việc.
