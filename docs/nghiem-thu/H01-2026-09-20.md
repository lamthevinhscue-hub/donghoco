# BIÊN BẢN GÓI H01 — THỐNG NHẤT HỒ SƠ ĐỂ KHÔNG GIAO SAI VIỆC

**Mã gói:** H01 (lượt 1) — danh mục DHC-H14-20260919, giao dịch TXN-20260919-02
**Ngày thực hiện:** 19/09/2026 (tên tệp đặt theo quy ước trong prompt giao: `H01-2026-09-20.md`)
**Trạng thái:** hoàn thành local — chờ GPT Work nghiệm thu độc lập
**HEAD lúc bắt đầu:** `a715fd0` (= `origin/main`, đã đối chiếu `git rev-parse HEAD origin/main`)
**Giới hạn:** gói tài liệu; không sửa website, nội dung xuất bản, cấu hình, dependency, ảnh, route. Không stage, không commit, không push, không deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi | Nội dung |
|---|---|---|
| `docs/CHI-MUC-KE-HOACH-HIEN-HANH.md` | Tạo mới | Chỉ mục điều phối: bộ hiện hành (mục 1), số liệu và quy tắc đã chốt (mục 2), bốn tài liệu nguồn (mục 3), tài liệu lịch sử (mục 4), prompt cũ hết hiệu lực (mục 5), nhóm dữ liệu/hồ sơ nguồn (mục 6), danh sách đề nghị commit tách khỏi bằng chứng nội bộ (mục 7), quy tắc duy trì (mục 8) |
| `docs/danh-gia-toan-dien-va-huong-phat-trien-2026-09-19.md` | Sửa (10 vị trí) | Đính chính theo mục 3.1 kế hoạch hợp nhất, chi tiết ở mục 2 của biên bản này |
| `docs/huong-dan-bat-do-luong-2026-09-19.md` | Sửa (7 vị trí) | Đính chính theo mục 3.2 |
| `docs/QUY-CHUAN-HINH-ANH.md` | Sửa (8 vị trí) | Đính chính theo mục 3.3 |
| `docs/prompt-dich-13-bai-lap-ngo-cut-en.md` | Sửa (9 vị trí) | Đính chính theo mục 3.4 |
| `docs/ra-soat-toan-bo-2026-09-05.md` | Sửa (1 vị trí) | Thêm hai câu liên kết điều phối ở mục 8; không viết lại nội dung lịch sử |
| `output/h01-plan-index-audit/` | Tạo mới | Bằng chứng kiểm kê (không đề nghị commit) |

Không đụng tệp nào khác; giữ nguyên toàn bộ untracked có trước. Đặc biệt: `docs/LO-TRINH-PHAT-TRIEN-HIEN-TAI.md` không được sửa — bộ giao việc H01 mục 2 có nhắc "cập nhật lộ trình hiện tại bằng liên kết", nhưng prompt giao H01 giới hạn phạm vi sửa ở bốn tài liệu nguồn và `ra-soat-toan-bo` tối thiểu; làm theo phạm vi hẹp hơn, liên kết tới lộ trình được đặt trong chỉ mục (mục 4).

## 2. Danh sách đính chính đã áp (đối chiếu mục 3 kế hoạch hợp nhất)

### 2.1. `danh-gia-toan-dien-va-huong-phat-trien-2026-09-19.md` (mục 3.1)

| Vị trí (sau sửa) | Nội dung đính chính |
|---|---|
| Đầu tệp | Thêm khối "Trạng thái tài liệu": nguồn đã thẩm định, giữ truy vết; lệnh thi công hiện hành là bộ giao việc + kế hoạch hợp nhất |
| Dòng nền đối chiếu | `521f5fa`/266 commit là mốc lúc rà; hiện hành `a715fd0`/268 commit; không dùng lại số cũ làm hiện hành |
| Mục 1.1 (262 tệp) | "Thư viện lớn nhất tiếng Việt" chưa có khảo sát đủ để xếp hạng; kết luận giữ được là kỷ luật ghi nguồn và giới hạn |
| Mục 1.3 (tiêu đề) | Bỏ nhận định "vượt chuẩn quốc tế" khỏi tiêu đề; so FHH là quan sát một trang một thời điểm |
| Mục 1.3 (kết đoạn) | Ghi chú tương tự tại câu "trang vượt tổ chức chuẩn của ngành" |
| Mục 2.2 (hệ quả EN) | Mốc chưa có bài EN không phải ngõ cút: `HistoryTimeline.astro` giữ link VI kèm nhãn "Vietnamese only — no English version yet"; vấn đề là thiếu bản dịch, không phải 404 |
| Mục 2.2 (kết đoạn) | "Mức quốc tế" là cảm nhận chưa có khảo sát |
| Mục 3.1 (tiêu đề và đầu mục) | 15 bài iconic thay bằng **13 bài = 7 iconic + 6 thương hiệu**, giao việc theo H03; danh sách 15 giữ để truy vết |
| Mục 5.2 (lý do giữ SVG) | Truy được nguồn là lợi thế; độ chính xác từng hình vẫn cần thẩm định riêng |
| Mục 4.4 (RSS/llms, CSP) | RSS là kênh trình đọc tin, không phải email, chân trang cần sửa cách nói khi làm H08; `llms.txt` hạ H13 ưu tiên thấp; Report-Only không "vô nghĩa" — hỗ trợ quan sát vi phạm, CSP thuộc H11 |
| PHẦN 6 (bảng thứ tự) | Hai dòng bổ sung đính chính: 13 bài theo H03; RSS là H08, llms.txt là H13 |
| PHẦN 7 (câu hỏi 2) | Ghi tập đúng 13 bài, ba đợt 5 + 2 + 6 |
| PHẦN 6 (kết) | Thiếu Search Console vẫn nghiệm thu được nhiều việc; không phải điều kiện tiên quyết của mọi việc còn lại |

### 2.2. `huong-dan-bat-do-luong-2026-09-19.md` (mục 3.2)

| Vị trí | Nội dung đính chính |
|---|---|
| Đầu tệp | Khối trạng thái tài liệu (đầu vào H02) + quy tắc chỉ ghi số quan sát thực tế |
| Sau bảng kiểm Analytics | Beacon HTTP 200 chỉ chứng minh một lần gửi được chấp nhận; chưa chứng minh dashboard có dữ liệu đầy đủ/tích lũy từ lâu; đường dẫn ngẫu nhiên là quan sát một thời điểm — không hardcode, không suy mục đích khi thiếu tài liệu xác nhận |
| Bước 3 (DNS) | Nơi thêm bản ghi TXT là nhà cung cấp DNS có nameserver có thẩm quyền — có thể khác nơi mua tên miền, có thể là Vercel |
| Bước 4 (sitemap) | Dùng URL đầy đủ `https://www.kienthucdonghoco.vn/sitemap-index.xml`; không hứa Success "sau vài phút" |
| Bước 5 (index) | Kiểm khả năng index, canonical, lý do loại trừ trước khi Request Indexing |
| Sau khi làm xong (bảng chờ) | Tên báo cáo hiện hành Pages/Page indexing, không dùng Coverage; các mốc ngày là ước lượng, không cam kết |
| Bốn con số (kết) | Phân tích EN theo nhóm URL `/en/`; quốc gia chiều phụ; không quyết định dừng EN từ một tuần ít dữ liệu; kỳ cơ sở 28 ngày |
| Ghi chú hồ sơ (cách đo) | `getEntriesByType('resource')` chỉ hỗ trợ tìm tài nguyên; đuôi `/view` tự nó chưa chứng minh analytics; cần xem Network với trạng thái, nội dung, initiator |

### 2.3. `QUY-CHUAN-HINH-ANH.md` (mục 3.3)

| Vị trí | Nội dung đính chính |
|---|---|
| Đầu tệp | "Ban hành" đọc đúng là **đề xuất trình duyệt, chưa được phê duyệt**; H04 hoàn thiện; khối trạng thái tài liệu |
| B1 (quy tắc màu nghĩa) | Đồng thau chỉ mang nghĩa năng lượng trong sơ đồ cơ chế; trong ảnh lịch sử còn thuộc bảng màu thẩm mỹ |
| B3 (khung hình) | Ba tỷ lệ khung và bốn cấp nét là mặc định, có ngoại lệ có lý do; nét mảnh kiểm ở kích thước hiển thị thật |
| B5 (chữ và id) | Ưu tiên nhãn HTML nhưng không khẳng định chữ SVG không tiếp cận được; không cấm toàn bộ `id` — được phép ID duy nhất theo instance (gradient, mask, title/description) kèm kiểm trùng, đúng cơ chế `WatchImage` từ G09-B; chữ trong SVG không tự gây trùng ID |
| B7 (alt) | Có ngoại lệ: hình trang trí hoặc hình có nội dung tương đương bên cạnh có thể ẩn (`alt=""`/`aria-hidden`) theo W3C WAI và đúng `CONTENT-GUIDE.md` mục 2.5; không bắt mọi hình alt không rỗng |
| B8 (phân loại AI) | Phân loại theo chức năng ảnh, không chỉ `claimLevel`; ba mốc `context` không tự động đủ điều kiện; đặt cạnh mốc là lựa chọn biên tập; không phải kết luận pháp lý cấm mọi ảnh có nhãn hiệu |
| B8 (quy cách tệp) | Cơ chế ưu tiên JPG nằm ở `HistoryTimeline.astro` (`hasImage`), không phải `lich-su.astro`; chưa có chú thích AI riêng cho JPG — thêm JPG chưa đủ, phải có loại ảnh, chú thích hai ngôn ngữ, hồ sơ; giữ SVG để truy vết; JPG thay SVG chỉ là thay ảnh, không tự tạo hai lớp |
| Phần C (checklist) | Checklist đọc kèm các đính chính; H04 phát hành lại thống nhất |

### 2.4. `prompt-dich-13-bai-lap-ngo-cut-en.md` (mục 3.4)

| Vị trí | Nội dung đính chính |
|---|---|
| Đầu tệp | Khối trạng thái: đầu vào chi tiết H03, không tự là lệnh thi công; phần prompt trong khung giữ nguyên, đọc kèm đính chính |
| Bảng đợt 2 (mốc 1936) | Mốc 1936 là Special Pilot's Watch; bài Mark XI năm 1948; dùng làm đọc thêm dòng phi công, không gọi Mark XI ra mắt 1936 |
| Frontmatter (`updated`) | Dùng ngày hoàn tất rà soát thực tế, không cố định `2026-09-19` |
| Frontmatter (nguyên tắc chung) | Giữ URL nguồn, enum, mã tham chiếu; dịch trường văn bản hiển thị; không giữ máy móc trường chứa tiếng Việt cần dịch |
| Frontmatter (relatedModels) | "Bỏ hẳn" đọc đúng là không thêm quan hệ giả; chưa có hai trường là hiện trạng, không phải lệnh cấm lâu dài |
| Ràng buộc chung | Đoạn khóa `bo-prompt-glm-v2.md` (lệnh tự push, quy tắc slug) không còn hiện hành; phát hành theo mục 8 bộ giao việc |
| Sau đợt 1 | "Chỉ đổi danh sách tệp" chỉ cho đợt 2; đợt 3 cần khuôn thương hiệu riêng |
| Kết tệp (số liệu trang chủ) | Sau ba đợt là 9 thương hiệu và **11** iconic (không phải 12); dòng số liệu trang chủ tự sinh từ collection, không cần gói sửa tay |
| Báo cáo (số từ) | Ngưỡng 25% không đáng tin để nghiệm thu; đối chiếu theo đoạn, luận điểm, số, tên riêng, nguồn, mức dè dặt; số từ chỉ tham khảo |

### 2.5. `ra-soat-toan-bo-2026-09-05.md`

Thêm đúng một đoạn hai câu ở mục 8 (dẫn bộ giao việc và chỉ mục); toàn bộ nội dung lịch sử giữ nguyên.

## 3. Kiểm tra đã chạy (kết quả thật)

| Kiểm | Lệnh | Kết quả |
|---|---|---|
| Liên kết nội bộ + whitespace/EOF 7 tệp H01 | `node output/h01-plan-index-audit/kiem-h01.mjs` | **exit 0, TONG_LOI = 0** (chi tiết trong `kiem-link-noi-bo.json`): 95 liên kết nội bộ, 0 hỏng (CHI-MUC 77, danh-gia 4, huong-dan 3, QUY-CHUAN 4, prompt-dich 4, ra-soat 3, biên bản 0); 0 dòng có dấu cách/tab cuối; 7/7 tệp kết thúc bằng newline. Dòng 3–4 của `ra-soat-toan-bo` là ngắt dòng cứng markdown có trước H01, đưa vào danh sách cho phép có giải thích ngay trong script |
| `git diff --check` | `git diff --check` | **exit 0, không kết quả** (`git-diff-check.txt`). Lưu ý: cả 9 tệp vùng H01 đều untracked nên lệnh này không phủ chúng — whitespace/EOF do script trên đảm nhiệm |
| Đối chiếu danh sách tệp | `git status --short` lọc 9 tệp | `git-status-h01.txt`: 9 tệp `??` (7 tệp H01 + 2 tệp điều phối có trước), **0 tệp tracked bị sửa** |
| Nền | `git rev-parse HEAD origin/main` | `a715fd0` cả hai (`nen-git.txt`) — khớp nền bắt buộc |
| Quét ký tự ngoài dải VN/EN | `grep -oP` dải cho phép trên 7 tệp | Chỉ phát hiện `·` (U+00B7) tại dòng 43 `QUY-CHUAN` — nội dung có trước, H01 không thêm ký tự nào ngoài dải |

Không chạy build: gói không ảnh hưởng website (theo prompt, không bắt buộc).

## 4. Giới hạn và điểm chưa giải quyết

- Chỉ mục lập trên danh sách 112 tệp `docs/*.md` tại 19/09/2026; nhóm `ho-so-nguon-*`, `goi-du-lieu-*`, `goi-thong-so-*` liệt kê theo nhóm, không từng tệp.
- Không quét/sửa toàn bộ thư mục `docs/` theo đúng phạm vi; nếu có tài liệu tháng 8 nào còn dẫn sai số lượng nằm ngoài phạm vi H01, chúng giữ nguyên và bị chi phối bởi mục 5 chỉ mục (không còn là lệnh hiện hành).
- `docs/LO-TRINH-PHAT-TRIEN-HIEN-TAI.md` còn số liệu mốc lần 22 — không sửa trong H01 (giải thích ở mục 1); cần lượt rà trạng thái riêng.
- Liên kết ngoài (W3C) chỉ dẫn theo kế hoạch hợp nhất, chưa kiểm nội dung trang nguồn trong gói này.

## 5. Đề nghị phát hành

Danh sách đề nghị lưu Git nằm ở mục 7 của `docs/CHI-MUC-KE-HOACH-HIEN-HANH.md` (7 tệp H01 + 2 tệp điều phối chưa theo dõi Git). `output/h01-plan-index-audit/` chỉ dùng nội bộ, không đề nghị commit. Chưa stage, chưa commit — chờ GPT Work nghiệm thu và lệnh phát hành riêng theo mục 8 bộ giao việc.
