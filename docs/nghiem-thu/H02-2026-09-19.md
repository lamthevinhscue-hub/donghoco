# BIÊN BẢN GÓI H02 — ĐO ĐƯỢC NGƯỜI ĐỌC VÀ NHU CẦU TÌM KIẾM

**Mã gói:** H02 — danh mục DHC-H14-20260919, giao dịch TXN-20260919-06
**Ngày thực hiện:** 19/09/2026 (+0700)
**Trạng thái:** hoàn thành phần kiểm công khai, **chờ tài khoản dashboard** — dừng đúng kết luận theo yêu cầu gói; không chặn gói tiếp theo. **Vòng sửa 1 (TXN-20260919-08) và vòng sửa 2 (TXN-20260919-10): đã sửa hồ sơ và công cụ tự kiểm theo phán quyết — kết quả sitemap đúng 289 URL gồm 67 URL `/en/`; checker bắt buộc đúng số, FAIL đã xác minh với fixture sai (mục 4, 5.2); chờ tái nghiệm thu.**
**HEAD lúc bắt đầu:** `23cc961` = `origin/main` (đã đối chiếu `git rev-parse`)
**Giới hạn:** không sửa mã nguồn, nội dung xuất bản, `vercel.json`, Analytics, CSP, DNS, Search Console, sitemap, `robots.txt`, dependency, cấu hình hay tài khoản ngoài máy; không tạo automation; không cài công cụ đo mới (trình duyệt tự động dùng hạ tầng kiểm sẵn có); không tạo lưu lượng hàng loạt. Chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi |
|---|---|
| `docs/DO-LUONG-CO-SO.md` | Tạo mới — bảng cơ sở 28 ngày tách Analytics/Search Console, trạng thái hệ đo, sitemap/canonical/DNS, nhịp theo dõi, việc cần chủ dự án, giới hạn |
| `docs/huong-dan-search-console-mot-trang-2026-09-19.md` | Tạo mới — hướng dẫn một trang: Domain property, TXT tại dotVNDNS, sitemap URL đầy đủ, Pages/Page indexing trước Request Indexing |
| `docs/nghiem-thu/H02-2026-09-19.md` | Tạo mới — biên bản này |
| `output/h02-measurement-baseline/` | Bằng chứng kỹ thuật (chỉ dùng nội bộ, không đề nghị commit) |

Không đụng tệp nào khác; giữ nguyên toàn bộ untracked có trước (gồm `poll-h02.mjs` ở root — tệp máy phiên điều phối, không thuộc gói).

## 2. Dữ liệu đã có (quan sát trực tiếp 19/09/2026)

**Analytics — trạng thái hệ đo (headless, 6 route VI + `/en/`, liệt kê toàn bộ request rồi mới lọc):**

- Script Analytics tải HTTP 200 loại `script` trên **6/6 route**, nguồn khởi tạo là tài liệu trang; `window.va` là hàm, `window.vam` = `production` (đo dài trên `/` và `/en/`).
- Beacon `/view` **không quan sát được** trong môi trường headless (kể cả đợi 6 giây). Ghi trung thực là giới hạn đo; không suy từ đó rằng hệ đo hỏng hay dashboard có dữ liệu. Bằng chứng beacon gửi được chấp nhận dừng ở quan sát trình duyệt thật sáng 19/09 (HTTP 200 — chỉ chứng minh một lần gửi).
- Đường dẫn ngẫu nhiên quan sát được ghi kèm nhãn "quan sát tại một thời điểm, không hardcode".

**Search Console — bằng chứng công khai:** nameserver thẩm quyền là `ns1/ns2.dotvndns.vn`; apex chưa quan sát thấy bản ghi TXT nào; HTML home không có thẻ `google-site-verification`. Hai kiểm này chỉ chứng minh **không quan sát được hai phương thức xác minh phổ biến** tại thời điểm tra; GLM **chưa có quyền truy cập tài khoản/property** để xác nhận trạng thái Search Console nói chung. `robots.txt` cho phép bot tìm kiếm.

**Sitemap/canonical:** `sitemap-index.xml` 200; sitemap con **289 URL, gồm 67 URL `/en/` và 222 URL tiếng Việt** (phép đếm đúng sau khi sửa lỗi công cụ — mục 4); canonical và hreflang hai chiều + `x-default` đúng trên hai route mẫu; không có meta robots chặn index.

## 3. Dữ liệu chưa có (ghi "chưa có dữ liệu", không coi là 0)

- Toàn bộ số dashboard Vercel Analytics (lượt xem theo URL, nhóm VI/`/en/`, nguồn, thiết bị) — **chưa truy cập** vì không có quyền; thao tác tối thiểu chủ dự án cần làm đã liệt kê ở mục 6 của `DO-LUONG-CO-SO.md`.
- Toàn bộ số Search Console (impressions, clicks, CTR, trang đích, truy vấn, Pages/Page indexing) — **chưa có quyền truy cập tài khoản/property để lấy số liệu và xác nhận trạng thái**; hướng dẫn một trang đã kèm.
- Không tự tạo lưu lượng, không xin mật khẩu hay mã xác thực.

## 4. Số URL `/en/` trong sitemap — lỗi phương pháp đã sửa, kết quả đúng 67

Kết luận đúng (khớp phán quyết GPT Work): sitemap `sitemap-0.xml` gồm **289 URL, trong đó 67 URL `/en/` và 222 URL tiếng Việt**. Không có phát hiện thiếu URL `/en/`; không đề nghị gói sửa sitemap.

**Lỗi của lượt đo đầu và nguyên nhân gốc (diễn giải siết lại theo phán quyết vòng 2):** lượt lập tài liệu ban đầu đếm ra "0 URL `/en/`" do hai nguyên nhân — (1) **phương pháp đếm không phù hợp**: tệp `sitemap-0.xml` chỉ có **một dòng (0 ký tự xuống dòng)** nên đếm bằng số dòng khớp (`grep -c`) trực tiếp trên tệp tối đa trả 1, không thể là số URL; đếm hợp lệ phải tách từng thẻ rồi đếm từng kết quả khớp; (2) pattern bắt đầu bằng dấu gạch (`/en/`) truyền qua Git Bash **bị biến đổi đối số** thành đường dẫn Windows (MSYS path conversion) nên không khớp. Không ghi bất kỳ kết quả `grep -c` nào là 67 trên XML một dòng; phép đếm hợp lệ và tái lập được chỉ có hai loại dưới đây:

| Cách đếm hợp lệ | Kết quả |
|---|---|
| Regex trong script tự kiểm `kiem-h02.mjs`: đếm thẻ `<loc>` cho tổng, thẻ `<loc>https://www.kienthucdonghoco.vn/en/` cho nhóm `/en/` — **bắt buộc** đúng 289/67, lệch là exit 1 | **289 tổng, 67 `/en/`** trên cả ba tệp (và FAIL đã xác minh với fixture sai số) |
| Đếm từng kết quả khớp bằng dòng lệnh: `grep -o '<loc>[^<]*</loc>' sitemap-0.xml` rồi `wc -l` cho tổng; kết quả đó lọc `'vn/en/'` rồi `wc -l` cho nhóm `/en/` | 289 và 67 |

Dữ liệu không đổi giữa các lần tải: ba tệp sitemap tải từ `www` (19:30, 19:57) và `donghoco1.vercel.app` (19:58, +0700) **byte-identical** — sha256 `12fda7fe89a6b7c20d3839ebe7af1442a3b822a647c2222bb6fc0377e7be56220`, 22.680 bytes, ETag `a5c067741eed27ee127cfde7a075d387`, Last-Modified 19/09 12:34:49 GMT = 19:34:49 +0700, `X-Vercel-Cache: HIT`. Số 222 VI + 67 EN khớp cấu trúc 290 trang HTML (222 VI + 67 EN + 1 trang 404) — chéo kiểm hợp lý.

Chi tiết phụ ghi nhận: hreflang `vi` trên trang `/en/history/` trỏ tới `/lich-su` thiếu dấu gạch cuối — URL vẫn 200, không phải liên kết gãy.

## 5. Kiểm tra đã chạy (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Network 6 route | playwright-cli run-code, liệt kê toàn bộ response + requestfailed, `networkidle` + 2,5 giây | exit 0; 21–29 request mỗi route; chi tiết `output/h02-measurement-baseline/ket-qua-network.json` |
| Phân tích Analytics từ dữ liệu thô | `node output/h02-measurement-baseline/phan-tich-network.mjs` | exit 0; `phan-tich-analytics.json` |
| Đo dài beacon + `window.va` | run-code đợi 6 giây + Performance Resource Timing | exit 0; `beacon-dai-raw.txt`: `va`=function, `vam`=production, 0 beacon |
| Sitemap — lần đo đầu 19:30 | `curl` sitemap-index + sitemap-0 | 200/200; 289 URL; phép đếm khi đó báo "0 URL `/en/`" — **kết quả sai do lỗi công cụ (mục 4)**; `sitemap-check.txt`, `sitemap-0.xml` |
| Sitemap — đo lại vòng sửa 1 | `curl` lại 19:57 và 19:58 (+0700): cùng URL, thêm `donghoco1.vercel.app`, kiểm header cache/ETag | Cùng tệp byte-identical (sha256 `12fda7fe…`, ETag `a5c06774…`, Last-Modified 19:34:49 +0700, `X-Vercel-Cache: HIT`); tự kiểm đếm đúng trên cả ba tệp: 289 URL, 67 URL `/en/` — `sitemap-0-lan2.xml`, `sitemap-0-vercelapp.xml` |
| Canonical/hreflang/robots meta | `curl` + grep 2 route mẫu | Đúng cặp; `route-vi.html`, `route-en.html` |
| DNS công khai | `nslookup` NS/TXT/A | `dns-kiem.txt`; NS dotVNDNS; apex chưa có TXT |
| `git diff --check` | `git diff --check` | Sạch, exit 0 |
| Liên kết nội bộ tài liệu mới + tự kiểm bắt buộc số sitemap | `node output/h02-measurement-baseline/kiem-h02.mjs` | exit 0, 0 hỏng; tự kiểm bắt buộc 289 tổng + 67 `/en/` trên 3 tệp sitemap đã tải (kết quả `kiem-link-noi-bo.json`) |
| Xác minh checker thất bại khi số liệu sai (vòng sửa 2) | `node kiem-h02.mjs fixture-sai-en.xml` (fixture 289 tổng, 66 `/en/`) | **exit 1**, báo `urlEn 66 khác 67`, `tongLoi: 1` (`log-checker-fixture-sai.txt`); chạy lại bản sạch exit 0 (`log-checker-binh-thuong.txt`, `log-checker-sau-fixture.txt`) |

Không chạy build: gói chỉ sửa hồ sơ.

## 5.1. Vòng sửa 1 (TXN-20260919-08) — tệp đã sửa trong vòng này

- `docs/DO-LUONG-CO-SO.md`: mục 3.1 và 3.2 đổi kết luận Search Console thành "chưa có quyền truy cập tài khoản/property..."; bảng 3.2 ghi rõ hai kiểm công khai chỉ phản ánh hai phương thức không quan sát được; mục 4 sửa số liệu sitemap thành 289 URL gồm 67 URL `/en/`, ghi phương pháp đếm đúng; loại đề nghị gói sửa sitemap; mục 7 đồng bộ giới hạn.
- `docs/huong-dan-search-console-mot-trang-2026-09-19.md`: bước 4 sửa thành phần sitemap đúng (222 tiếng Việt + 67 tiếng Anh).
- `docs/nghiem-thu/H02-2026-09-19.md`: các mục 2, 3, 4, 5, 5.1, 6, dòng trạng thái sửa theo phán quyết và bản phân tích lỗi đếm.
- `output/h02-measurement-baseline/`: thêm `sitemap-0-lan2.xml`, `sitemap-0-vercelapp.xml`; `kiem-h02.mjs` thêm tự kiểm đếm tổng + `/en/` (kết quả ghi trong `kiem-link-noi-bo.json`).

## 5.2. Vòng sửa 2 (TXN-20260919-10) — sửa hẹp trên vòng 1

- `output/h02-measurement-baseline/kiem-h02.mjs`: từ "chỉ tính và in số" thành **tự kiểm bảo vệ số liệu** — mỗi sitemap bắt buộc `tongUrl === 289` và `urlEn === 67`; lệch thì `tongLoi` tăng, JSON nêu tệp và số thực tế (trường `LOI`), exit 1; hỗ trợ tham số đường dẫn tệp để kiểm fixture.
- Xác minh hai chiều: chạy bản sạch exit 0; fixture `fixture-sai-en.xml` (289 tổng, 66 `/en/`) **exit 1** với thông báo `urlEn 66 khác 67`; chạy lại bản sạch exit 0 — log tại `log-checker-binh-thuong.txt`, `log-checker-fixture-sai.txt`, `log-checker-sau-fixture.txt`.
- `docs/DO-LUONG-CO-SO.md` và mục 4 biên bản này: sửa lại diễn giải nguyên nhân — bỏ mọi kết quả `grep -c` = 67 (không thể đúng trên XML một dòng vì `grep -c` đếm số dòng khớp); ghi đủ hai nguyên nhân của kết quả 0 sai: phương pháp đếm không phù hợp (tệp một dòng) và biến đổi đối số bắt đầu bằng `/` trong Git Bash (MSYS path conversion); minh họa dòng lệnh chỉ dùng đếm từng kết quả khớp (`grep -o` rồi `wc -l`).
- Số liệu giữ nguyên: 289 URL gồm 222 VI và 67 `/en/`; Search Console giữ đúng mức "chưa có quyền truy cập để xác nhận trạng thái/lấy số liệu".

## 6. Giới hạn và điểm chưa giải quyết

- Phần dashboard chưa truy cập được — kết luận đúng là "hoàn thành phần kiểm công khai, chờ tài khoản"; không chặn gói tiếp theo.
- Trạng thái xác minh Search Console chỉ dừng ở "không quan sát được hai phương thức công khai"; chưa có quyền để xác nhận trạng thái.
- Beacon lượt xem không quan sát được trong headless — đã ghi nguồn bằng chứng thay thế và giới hạn của nó.
- Bài học kiểm chứng đã ghi vào hồ sơ: pattern grep bắt đầu bằng dấu gạch bị MSYS chuyển thành đường dẫn Windows trong Git Bash — đếm sitemap phải qua script tự kiểm.
- Mọi số liệu là chụp tại 19/09/2026; cập nhật kỳ sau phải đo lại, không kế thừa.

## 7. Đề nghị lưu Git khi H02 đạt

**Tệp tài liệu (3):** `docs/DO-LUONG-CO-SO.md`, `docs/huong-dan-search-console-mot-trang-2026-09-19.md`, `docs/nghiem-thu/H02-2026-09-19.md`.
**Chỉ dùng nội bộ, không đề nghị commit:** `output/h02-measurement-baseline/` (24 tệp: 2 script đo + script phân tích + script kiểm có bắt buộc số, fixture sai số và 3 log xác minh checker, dữ liệu thô network/beacon, JSON phân tích và kết quả kiểm, 3 tệp sitemap tải, sitemap-index, robots.txt, DNS, 2 HTML route mẫu + home, sitemap-check).

Gói tiếp theo đề nghị theo thứ tự bộ giao việc: **H04** (H03-A cũng đã đủ điều kiện mở vì phụ thuộc H01 đã đóng — chờ GPT Work chọn thứ tự gửi). Chưa tự thực hiện.
