# BIÊN BẢN GÓI H03-A — DỊCH 5 BÀI MẪU BIỂU TƯỢNG SANG TIẾNG ANH

**Mã gói:** H03-A (lượt 1/3 của H03) — danh mục DHC-H14-20260919, giao dịch TXN-20260919-30
**Ngày thực hiện:** 19/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work tái nghiệm thu. **Vòng sửa 1 (TXN-20260919-32): sửa `kiem-h03a.mjs` (bỏ `refsVi` gây `ts(6133)`). Vòng sửa 2 (TXN-20260919-34): thay Pagefind hình thức bằng kiểm tìm kiếm thực qua API trên preview. Vòng sửa 3 (TXN-20260919-36): gỡ bullet "Tech Gombessa (2013)" khỏi bài EN (dữ kiện nghi vấn CAN-KIEM-CHUNG 56 không được xuất bản) + checker chặn cứng chuỗi trong src và dist + mutation chứng minh bắt được khi chèn lại; không sửa bài VI, template, package.**
**HEAD lúc bắt đầu:** `d7f8fef` = `origin/main`, nhánh `main`
**Giới hạn:** không sửa bài VI, template, component, schema, route framework, CSS, dữ liệu khác, ảnh, package/config; không mở H03-B/H03-C; chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi |
|---|---|
| `src/content/mauIconic/en/royal-oak.md` | Tạo mới — dịch từ bài VI |
| `src/content/mauIconic/en/patek-nautilus.md` | Tạo mới — dịch từ bài VI |
| `src/content/mauIconic/en/reverso.md` | Tạo mới — dịch từ bài VI |
| `src/content/mauIconic/en/fifty-fathoms.md` | Tạo mới — dịch từ bài VI |
| `src/content/mauIconic/en/zenith-el-primero.md` | Tạo mới — dịch từ bài VI |
| `src/i18n/contentRoutes.ts` | Thêm đúng 5 dòng `ARTICLE_PAIRS` vào khối "Mẫu iconic" (đầu khối, sau dòng 105) |
| `scripts/check-g05-history-journey.mjs` | **Cập nhật kỳ vọng G5-6 và G5-D2** — chi tiết mục 3 |
| `CAN-KIEM-CHUNG.md` | Thêm mục 55 (lỗi "thanhläng" bài VI reverso) và mục 56 (nghi vấn năm Tech Gombessa); mục 56 cập nhật vòng sửa 3: bản EN đã gỡ mốc năm nghi vấn, VI còn hiển thị chờ nguồn |
| `output/h03-a-iconic-en/` | Bằng chứng nội bộ (không đề nghị commit) |

## 2. Quyết định biên tập chính

- **`category` giữ giá trị enum tiếng Việt** (`sport-luxury`, `dress`, `lặn`, `chronograph`) — khóa phân loại trong schema; nhãn hiển thị do template map theo ngôn ngữ (`getIconicCategoryLabel`).
- **Bỏ `relatedModels`/`relatedMechanisms`** ở cả 5 tệp EN — nhất quán với 4 tệp EN hiện có (0 tệp có); template `IconicArticle.astro` resolve slug theo ngôn ngữ hiện hành nên slug EN chưa tồn tại sẽ vô tác dụng, và trường `relation` tiếng Việt sẽ lọt nếu hiển thị. Cân nhắc thêm lại bằng dữ liệu thật sau khi các bài liên quan có bản EN (H03-B/C).
- **Liên kết thân bài**: duy nhất bài fifty-fathoms có liên kết thân bài — chuyển 2 đích sang EN đã xuất bản (`/en/iconic-watches/rolex-submariner/`, `/en/history/`); 4 bài còn lại không có liên kết thân bài trong bản VI nên không tạo mới.
- **`updated` = 2026-09-19** (ngày soát thực tế); `date` giữ nguyên bản VI.
- **Số từ không dùng làm tiêu chí**: đối chiếu theo đoạn ở bảng claim (`doi-chieu-claim.md`); số từ thật chỉ tham khảo (EN thấp hơn VI 10–14% do đặc thù đếm từ tiếng Việt).

## 3. Cập nhật kỳ vọng kiểm G5-6/G5-D2 (nêu rõ — điểm cần phán quyết xác nhận)

Chuỗi `npm run check` lần đầu **thất bại ở script G05** (`scripts/check-g05-history-journey.mjs`): kỳ vọng cứng từ G05-A "12 mốc có cặp EN + 13 mốc chỉ VI + 3 mốc chưa có bài" (mức nguồn G5-6 và mức dist G5-D2). H03-A thêm đúng 5 cặp hợp lệ → thực tế **17/8/3**. Theo quy tắc chung mục 3.3 ("dữ liệu hợp lệ thay đổi số lượng — giải thích và cập nhật kiểm tra tương ứng") đã cập nhật **chỉ hai dòng kỳ vọng** trong script G05 (12→17, 13→8; kèm bình luận ghi rõ lý do H03-A 19/09/2026). Đây là tệp sửa ngoài danh sách phạm vi liệt kê của prompt (không rơi vào mục cấm nêu tên) — ghi rõ để GPT Work phán quyết; không sửa nó thì tiêu chí "check đạt" không thể đáp ứng.

## 4. Kết quả kiểm tra (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Chuỗi kiểm hiện hành (chạy lại vòng sửa 1) | `npm run check` | **exit 0** — astro check **373 files: 0 errors, 0 warnings, 0 hints**; không còn `ts(6133)`; toàn bộ script (scan-chars, 25+ script cụm, G02–G06…) ĐẠT. Lưu ý trung thực: lần chạy trước báo "368 files 0/0/0" đúng tại thời điểm chạy nhưng **chưa phủ `kiem-h03a.mjs` vì tệp kiểm được tạo sau lần chạy đó** — lỗi trình tự đã ghi nhận |
| Build (chạy lại vòng sửa 1) | `npm run build` | **exit 0** — 295 trang HTML (+5), Pagefind indexed 295, "Đã quét 295 trang HTML, 21612 link" 0 hỏng; sitemap **294 URL** (+5); G5-D2 ĐẠT 17/8/3 |
| Route EN 5 bài | `dist/en/iconic-watches/<slug>/` | 9 thư mục bài (4 cũ + 5 mới) — **khớp kỳ vọng 9 bài EN iconic**; sitemap 294 URL (+5), 10 URL nhóm iconic-watches (9 bài + trang index) |
| Đối chiếu dist 5 cặp | `node output/h03-a-iconic-en/kiem-dist.mjs` | **exit 0, 51/51 ĐẠT** — H1 EN đúng bài; thân EN không rò câu VI (loại nhãn ngôn ngữ "Tiếng Việt" của switcher — có chủ đích); canonical tự trỏ cả VI/EN; hreflang hai chiều trên cả hai phía; switcher có link về bài VI |
| Timeline đọc tiếp | G5-D2 (trong build) + `kiem-dist.mjs` | `/en/history/` có 17 liên kết đích EN; 5 mốc royal-oak/patek-nautilus/reverso/fifty-fathoms/zenith-el-primero trỏ đúng route EN mới |
| Pagefind | Build log "Pagefind indexed 295 pages" + `pagefind-entry.json` tồn tại | ĐẠT |
| Trình duyệt cặp đại diện fifty-fathoms | playwright trên preview `astro preview` (port 4327), script `kiem-trinh-duyet.mjs` + `kiem-dark.mjs` + `kiem-dark-mau.mjs` | **11 ca ĐẠT**: desktop 1280 + mobile 375; H1 EN đúng, không rò VI; link thân bài Submariner EN count=1; không tràn ngang cả 4 ca; switcher EN→VI→EN về đúng path (không về trang chủ); dark mode: class `dark` áp đúng theo `prefers-color-scheme`, màu chữ H1 đổi `rgb(35,74,115)` sáng → `rgb(238,240,237)` tối |
| Kiểm gói (chạy lại vòng sửa 1) | `node output/h03-a-iconic-en/kiem-h03a.mjs` | **exit 0** — cấu trúc heading VI=EN từng bài; custom_slug khớp; updated đúng ngày; thân EN thuần EN; 0 relatedModels; URL nguồn giữ nguyên 100%; references giữ nguyên từng ký tự; không trailing whitespace; EOF newline đủ |
| Checker G05 chạy riêng (vòng sửa 1) | `node scripts/check-g05-history-journey.mjs` | exit 0 — KẾT LUẬN ĐẠT 11/11 ca |
| Kiểm dist (chạy lại vòng sửa 2) | `node output/h03-a-iconic-en/kiem-dist.mjs` | exit 0 — kiểm tĩnh 46 ca ĐẠT; **đã bỏ hoàn toàn mục Pagefind hình thức** (file index tồn tại không được tính là Pagefind đạt — ghi chú trong JSON kết quả dẫn tới runner riêng) |
| **Tìm kiếm Pagefind thực (vòng sửa 2)** | `kiem-pagefind.mjs` qua playwright trên preview: gọi API `/pagefind/pagefind.js`, 5 truy vấn đặc trưng + 1 ca âm | **6/6 ĐẠT** — chi tiết mục 4.2 |
| `git diff --check` | `git diff --check` | Sạch, exit 0 |

## 4.1. Vòng sửa 1 (TXN-20260919-32) — sửa script bằng chứng, không đụng nội dung dịch

- **Bằng chứng GPT Work đúng:** `kiem-h03a.mjs` dòng 53 khai báo `refsVi` không dùng — `astro check` quét cả `output/*.mjs` và báo `ts(6133)`. Nguyên nhân trình tự: tệp kiểm được tạo **sau** lần chạy `npm run check` cuối của lượt đầu, nên con số "368 files 0/0/0" không phủ nó.
- **Đã sửa:** bỏ dòng khai báo `refsVi`; logic kiểm references không đổi (so sánh nguyên dòng `references: […]` giữa VI và EN qua `thamChieuVi`).
- **Đã chạy lại:** `npm run check` (373 files, 0/0/0), `npm run build` (295 trang / 294 sitemap / 21.612 link), `check-g05-history-journey.mjs` riêng (ĐẠT 11/11), `kiem-h03a.mjs` (exit 0), `kiem-dist.mjs` (51/51), `git diff --check` (sạch). Log bằng chứng đã thay thế bằng lần chạy mới (`log-check.txt`, `log-build.txt`).
- Không đụng: 5 tệp EN, `contentRoutes.ts`, checker G05, `CAN-KIEM-CHUNG.md`, bài VI.

## 4.2. Vòng sửa 2 (TXN-20260919-34) — kiểm tìm kiếm Pagefind thực

**Phân biệt hai phép kiểm (theo yêu cầu phán quyết):**

- **Kiểm dist tĩnh** (`kiem-dist.mjs`, 46 ca): H1, rò VI, canonical, hreflang hai chiều, switcher link, timeline read-more, frontmatter, whitespace/EOF — chạy trên tệp `dist/` không cần HTTP.
- **Kiểm tìm kiếm thực** (`kiem-pagefind.mjs`, 6 ca): chạy qua trình duyệt trên preview `astro preview` (port 4328), gọi **API Pagefind thật** `import('/pagefind/pagefind.js')` rồi `pf.search(<truy vấn>)`, lấy `data()` của 10 kết quả đầu và so `data.url` với route kỳ vọng.

**Kết quả từng ca (log `log-pagefind.txt`):**

| Loại | Truy vấn | Số kết quả | Kết quả đầu (URL) | Route H03-A trong kết quả | ĐẠT |
|---|---|---:|---|---|---|
| Dương | Royal Oak octagonal bezel | 1 | `/en/iconic-watches/royal-oak/` | Có | ĐẠT |
| Dương | Nautilus porthole case | 2 | `/en/iconic-watches/patek-nautilus/` | Có | ĐẠT |
| Dương | Reverso flip mechanism | 1 | `/en/iconic-watches/reverso/` | Có | ĐẠT |
| Dương | Fifty Fathoms Maloubier divers | 1 | `/en/iconic-watches/fifty-fathoms/` | Có | ĐẠT |
| Dương | El Primero automatic chronograph | 2 | `/en/iconic-watches/zenith-el-primero/` | Có | ĐẠT |
| **Âm** | `xyzquuxblorpnhi` (vô nghĩa) | 1 | `/en/mechanisms/anti-magnetism/` | **Không** (0/5 route) | ĐẠT |

Phát hiện hành vi được ghi lại: **Pagefind 1.x với truy vô nghĩa không trả 0 kết quả** — nó trả một kết quả best-effort (`/en/mechanisms/anti-magnetism/`). Vì vậy tiêu chí ca âm đặt đúng theo phán quyết là "truy vô nghĩa không bị nhận nhầm làm bằng chứng cho một trong năm route": kết quả không chứa route nào trong 5 route H03-A. Nếu cần ràng buộc chặt hơn (trả đúng 0 kết quả) thì đó là hành vi thư viện Pagefind, không thuộc phạm vi sửa của gói.

Một ca phụ: lần chạy đầu của ca âm gặp lỗi import nhất thời (`soKetQua: -1`) — chạy lại riêng (`kiem-pagefind-am.mjs`, `log-pagefind-am.txt`) import bình thường; chạy chốt trong `kiem-pagefind.mjs` đã thành công đủ 6 ca.

Cũng sửa trong vòng này: biến `routes` truyền vào `page.evaluate` nhưng không dùng gây `ts(6133)` hint (astro check 375 files) — đã bỏ; chạy lại `npm run check`: **375 files, 0 errors, 0 warnings, 0 hints**; `npm run build` exit 0 (295 trang / sitemap 294 / 21.612 link).

## 4.3. Vòng sửa 3 (TXN-20260919-36) — gỡ dữ kiện nghi vấn khỏi EN, checker chặn cứng

**Bản chất vòng sửa:** thu hẹp để tránh xuất bản dữ kiện đang nghi vấn (`Tech Gombessa (2013)` — CAN-KIEM-CHUNG mục 56), **không phải xác nhận năm mới**. Không thay bằng năm khác, không suy đoán.

- `src/content/mauIconic/en/fifty-fathoms.md`: gỡ bullet "- **Tech Gombessa** (2013) — adds the 'Gombessa' function…"; mục còn 4 tham chiếu có năm đã kiểm. Tên "Tech Gombessa" **giữ trong `references` frontmatter** (VI và EN) vì chỉ là tên mẫu, không kèm khẳng định năm hay dữ kiện chưa xác minh.
- `CAN-KIEM-CHUNG.md` mục 56: cập nhật — bản EN **không công bố** mốc năm chờ nguồn; **bài VI vẫn hiển thị "(2013)"** (ngoài phạm vi sửa của gói EN, chờ biên tập); khi có nguồn chính hãng **sửa đồng thời VI và EN cùng lượt** (EN cân nhắc khôi phục bullet với năm đã xác minh).
- `output/h03-a-iconic-en/kiem-h03a.mjs`: thêm ca chặn cứng chuỗi `Tech Gombessa (2013)` ở **src EN** (chuẩn hóa `**` của Markdown) và **dist EN** (bóc thẻ HTML + chuẩn hóa khoảng trắng — vì render là `<strong>Tech Gombessa</strong> (2013)`).

**Bằng chứng checker hoạt động hai chiều:**

| Ca | Kết quả |
|---|---|
| Checker chạy trên **dist cũ** (chưa build lại, dist còn chuỗi nghi vấn sau bóc thẻ) | **exit 1** — bắt đúng ca `chan-nam-nghi-van dist EN` (`log-goi-truoc-build.json`) |
| Build lại sau gỡ bullet | exit 0 — 295 trang / 21.612 link |
| Checker chạy trên **src + dist mới** | **exit 0** |
| **Mutation**: chèn lại `- **Tech Gombessa** (2013) …` vào EN (đúng cú pháp Markdown), chạy checker | **exit 1** — bắt đúng `chan-nam-nghi-van src EN` (`log-mutation.json`) |
| Khôi phục từ bản gốc (`cmp` khớp từng byte) | exit 0 — checker ĐẠT trở lại |

Ghi chú kỹ thuật hai lỗ hổng của phép tìm chuỗi đã vá trong vòng này: (1) chuỗi render có thẻ `</strong>` xen giữa nên phải bóc thẻ; (2) sau bóc thẻ còn thừa hai dấu cách nên phải chuẩn hóa khoảng trắng; (3) nguồn Markdown có `**` chen giữa nên phải bỏ nhấn mạnh trước khi tìm.

**Chạy lại cuối vòng:** `npm run check` exit 0 (375 files, 0/0/0); `npm run build` exit 0 (295 trang / sitemap 294 / 21.612 link); **Pagefind thật 6/6 ĐẠT** (5 truy vấn đặc trưng trúng đúng route EN + ca âm không chạm route nào trong 5); `kiem-h03a.mjs` exit 0; `git diff --check` sạch. Không sửa bài VI, template, package/config; không mở H03-B/C.

## 5. Số EN thực tế sau lượt này

**9/69 bài mẫu biểu tượng có bản EN**: rolex-submariner, omega-speedmaster, cartier-tank, rolex-gmt-master (4 cũ) + royal-oak, patek-nautilus, reverso, fifty-fathoms, zenith-el-primero (5 mới) — **khớp kỳ vọng 9 bài** sau H03-A.

## 6. Nghi vấn và phát hiện nguồn (CAN-KIEM-CHUNG)

- **Mục 55 — CẦN ƯU TIÊN KIỂM CHỨNG**: bài VI reverso, mục "Vị trí trong lịch sử" có lỗi font "thanhläng" (phải là "thanh lịch"), đang hiển thị sai trên `/mau-iconic/reverso/`. Bản EN dịch đúng ý. Ngoài phạm vi sửa H03-A — cần lượt biên tập riêng.
- **Mục 56 — CHỜ NGUỒN**: nghi năm ra mắt "Tech Gombessa (2013)" trong bài fifty-fathoms là sai (có thể 2022 kỷ niệm 70 năm). **Bản EN đã gỡ bullet có mốc "Tech Gombessa (2013)" (vòng sửa 3) — không công bố năm nghi vấn; bản VI vẫn còn mốc này và chờ nguồn chính hãng.** Khi có nguồn: sửa đồng thời VI và EN cùng lượt (EN cân nhắc khôi phục bullet với năm đã xác minh).

## 7. Ca chưa kiểm được

- Pagefind **đã kiểm bằng API thật trên preview** (vòng sửa 2, 6 ca) — mức chắc hơn HTTP 200; nhưng **gõ truy vấn trong giao diện tìm kiếm người dùng** (Pagefind UI) vẫn chưa thử thủ công.
- Screen reader thật chưa kiểm (giới hạn đã ghi từ các gói trước).
- Chỉ kiểm trình duyệt sâu với cặp đại diện fifty-fathoms theo yêu cầu "ít nhất một cặp"; 4 cặp còn lại đã phủ ở mức dist (H1, rò VI, canonical/hreflang, switcher link, Pagefind API) nhưng chưa từng ca trình duyệt.

## 8. Đề nghị phát hành

**Tệp đề nghị lưu Git khi H03-A đạt (7):**
1. `src/content/mauIconic/en/royal-oak.md`
2. `src/content/mauIconic/en/patek-nautilus.md`
3. `src/content/mauIconic/en/reverso.md`
4. `src/content/mauIconic/en/fifty-fathoms.md`
5. `src/content/mauIconic/en/zenith-el-primero.md`
6. `src/i18n/contentRoutes.ts`
7. `docs/nghiem-thu/H03-A-2026-09-19.md`
**Kèm theo (cần GPT Work xác nhận đưa vào cùng đợt):** `scripts/check-g05-history-journey.mjs` (bắt buộc để chuỗi check đạt trên main — không có nó thì main sau deploy sẽ fail check), `CAN-KIEM-CHUNG.md` (2 mục mới).
**Chỉ nội bộ:** `output/h03-a-iconic-en/` (**23 tệp = 1 bảng + 7 script + 15 log**, tổng khớp):
- 1 bảng: `doi-chieu-claim.md`;
- 7 script kiểm: `kiem-h03a`, `kiem-dist`, `kiem-pagefind`, `kiem-pagefind-am`, `kiem-trinh-duyet`, `kiem-dark`, `kiem-dark-mau` (đuôi `.mjs`);
- 15 log: `log-check`, `log-build`, `log-dist`, `log-goi`, `log-goi-truoc-build.json`, `log-mutation`, `log-g05-rieng`, `log-pagefind`, `log-pagefind-am`, `log-preview`, `log-preview2`, `log-trinh-duyet`, `log-dark`, `log-dark-mau`, `log-goi-truoc-build.txt` (bản chạy đầu khi checker chưa vá — giữ làm vết, kết quả có giá trị là bản `.json`).
Gói kế: H03-B (freak, iwc-mark-xi). Chưa tự thực hiện.
