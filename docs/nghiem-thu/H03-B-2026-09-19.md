# BIÊN BẢN GÓI H03-B — DỊCH TIẾP FREAK VÀ IWC MARK XI SANG TIẾNG ANH

**Mã gói:** H03-B (lượt 2/3 của H03) — danh mục DHC-H14-20260919, giao dịch TXN-20260919-42
**Ngày thực hiện:** 19–20/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work tái nghiệm thu. **Vòng sửa 1 (TXN-20260919-44): gỡ cả hai calibre mâu thuẫn `32111`/`35111` khỏi bản EN (frontmatter bỏ trường `movement` — schema optional, SpecTable tự ẩn dòng rỗng; thân bài gỡ câu "calibre 35111"); checker chặn cứng hai chuỗi ở src EN + dist EN với mutation từng chuỗi hai chiều; CAN-KIEM-CHUNG mục 57 cập nhật đúng hiện trạng. Vòng sửa 2 (TXN-20260919-46): sửa câu sai còn sót ở mục 5; làm lại mutation NGOÀI cây repo chính bằng cây temp tương đương — repo không bị động trong toàn bộ quá trình mutation.**
**HEAD lúc bắt đầu:** `0cd753b` = `origin/main`, nhánh `main`
**Giới hạn:** không sửa bài VI, CSS, package/config, bài ngoài phạm vi; không thêm thông số/ca ngợi/suy luận thị trường/quan hệ bài giả; chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi |
|---|---|
| `src/content/mauIconic/en/freak.md` | Tạo mới — dịch từ bài VI |
| `src/content/mauIconic/en/iwc-mark-xi.md` | Tạo mới — dịch từ bài VI |
| `src/i18n/contentRoutes.ts` | Thêm đúng 2 cặp `ARTICLE_PAIRS` (đầu khối "Mẫu iconic") |
| `scripts/check-g05-history-journey.mjs` | Cập nhật kỳ vọng **G5-6 và G5-D2: 17/8/3 → 19/6/3** (kèm bình luận H03-B; dòng comment đầu tệp đồng bộ) |
| `src/data/timeline.json` | Thêm `readMoreNote`/`readMoreNote_en` **chỉ tại mốc `iwc-pilot` (1936)** — làm rõ bài đọc thêm là Mark XI 1948, không phải Special Pilot's Watch 1936 |
| `src/components/history/HistoryTimeline.astro` | Thêm 2 trường optional vào type `Milestone`; hàm `readMoreNoteOf`; render ghi chú (chỉ khi có) dưới link đọc thêm |
| `CAN-KIEM-CHUNG.md` | Thêm mục 57: mâu thuẫn calibre 32111/35111 trong bài VI iwc-mark-xi |
| `docs/nghiem-thu/H03-B-2026-09-19.md` | Tạo mới — biên bản này |
| `output/h03-b-iconic-en/` | Bằng chứng nội bộ (không đề nghị commit) |

## 2. Quyết định biên tập chính

- `category` giữ enum VI (`dress`, `pilot`) — khóa schema; `custom_slug` thêm; `updated` = **2026-09-20** (ngày rà soát thực tế); `power_reserve` dịch đơn vị ("7 ngày (168 giờ)" thành "7 days (168 hours)", "72 giờ" thành "72 hours"); `references`, `year`, `brand`, `movement` giữ nguyên; sources giữ URL, dịch label VI.
- **Bỏ `relatedModels`/`relatedMechanisms`** ở cả hai tệp EN — nhất quán các bài EN hiện có; template resolve slug theo ngôn ngữ nên các đích EN tương ứng chưa đủ; trường `relation` tiếng Việt sẽ lọt nếu hiển thị.
- **Liên kết thân bài**: cả hai bài VI không có liên kết thân bài Markdown nên không tạo mới.
- **Ghi chú mốc 1936 (làm rõ theo yêu cầu)**: mốc `iwc-pilot` (1936) có `readMore` trỏ bài Mark XI (1948). Theo mục 3.4 kế hoạch hợp nhất và H03 mục 6, thêm hai trường `readMoreNote`/`readMoreNote_en` **chỉ cho mốc này** và render trong `HistoryTimeline` (dòng chữ nghiêng dưới link "Read more"). Không sửa năm 1936, năm 1948, nguồn, mô tả lịch sử gốc; các mốc khác không có trường này nên hiển thị không đổi.
- **CAN-KIEM-CHUNG mục 57 (mới)**: mâu thuẫn nội bộ trong bài VI iwc-mark-xi — frontmatter `movement: "Calibre 32111"` đối lập thân bài "calibre 35111". *(Vòng sửa 1: bản EN đã gỡ cả hai — không còn công bố calibre mâu thuẫn; bài VI nguyên trạng chờ nguồn; chi tiết mục 4.1.)*

## 3. Kết quả kiểm tra (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Chuỗi kiểm hiện hành | `npm run check` | **exit 0** — astro check 375 files: 0 errors, 0 warnings, 0 hints; G5-6 ĐẠT **19 mốc có cặp EN + 6 chỉ VI + 3 chưa có bài** |
| Build | `npm run build` | **exit 0** — **297 trang HTML** (+2), Pagefind indexed 297, sitemap **296 URL** (+2), "Đã quét 297 trang HTML, 21802 link" |
| Số bài iconic EN | `ls dist/en/iconic-watches/` | **11 thư mục bài** (9 cũ + 2 mới) — khớp kỳ vọng 11 bài EN |
| Checker G05 riêng | `node scripts/check-g05-history-journey.mjs` | exit 0 — ĐẠT 11/11 ca (kể cả G5-D2 dist 19/6/3) |
| Kiểm dist 2 cặp | `node output/h03-b-iconic-en/kiem-dist.mjs` | **exit 0, 21/21 ĐẠT** — H1 EN đúng bài; thân EN không rò VI; canonical/hreflang hai chiều; switcher link về bài VI; timeline trỏ 2 đích EN mới; **ghi chú "the Mark XI launched in 1948 — not this milestone's 1936 Special Pilot's Watch" hiển thị ở trang lịch sử EN và tương ứng ở trang VI**; không có "Mark XI (1936)" hay "1936 Mark XI" |
| Pagefind thật | `kiem-pagefind.mjs` qua playwright trên preview, API `/pagefind/pagefind.js` | **3/3 ĐẠT**: "Freak Ulysse Nardin Baselworld" → 1 kết quả = `/en/iconic-watches/freak/`; "Mark XI RAF soft iron cage" → 1 kết quả = `/en/iconic-watches/iwc-mark-xi/`; ca âm `qqzzxxvvkkt` → **0 kết quả** (`log-pagefind.txt`) |
| Kiểm gói | `node output/h03-b-iconic-en/kiem-h03b.mjs` | **exit 0** — heading VI=EN từng bài; custom_slug; updated đúng ngày; thân EN thuần EN; 0 relatedModels; URL nguồn 100%; references giữ nguyên; whitespace/EOF sạch; ca âm Pagefind xác nhận từ log |
| `git diff --check` | `git diff --check` | Sạch, exit 0 |

**Ghi chú thí nghiệm Pagefind (đã ghi log `log-pagefind-thu-nghiem.txt`):** Pagefind 1.x có hành vi best-effort không nhất quán với truy vô nghĩa — `xyzqvblorpquux` trả 2 kết quả (trong đó có `/en/iconic-watches/freak/`), nhưng `qqzzxxvvkkt` và `zqxkvbb` trả đúng 0. Ca âm chốt dùng truy đã xác nhận trả 0 (`qqzzxxvvkkt`) — tiêu chí "0 kết quả" chặt hơn tiêu chí "không chứa route". Truy vấn freak ban đầu dùng "carrousel" (chính tả VI) trả 0 vì bài EN viết "carousel" — truy vấn chốt dùng từ có trong bài.

## 4. Vòng sửa và kết quả số EN

### 4.1. Vòng sửa 1 (TXN-20260919-44) — gỡ calibre mâu thuẫn khỏi EN

- **`src/content/mauIconic/en/iwc-mark-xi.md`:** bỏ trường `movement: "Calibre 32111"` khỏi frontmatter (schema `movement` là optional; SpecTable tự ẩn dòng rỗng — đã đối chiếu `IconicArticle.astro`) và gỡ câu "The current Mark XVIII uses the **calibre 35111** (based on the ETA 2892)." khỏi mục "The movement". Không thay bằng calibre khác, không suy đoán. Calibre có nguồn từ bản VI (calibre 89, JLC 889, ETA 2892, ETA 37524) giữ nguyên.
- **`CAN-KIEM-CHUNG.md` mục 57:** cập nhật — EN không còn công bố hai calibre mâu thuẫn; bài VI nguyên trạng vẫn hiển thị cả hai, chờ nguồn chính hãng; khi có nguồn sửa đồng thời VI/EN.
- **`kiem-h03b.mjs`:** thêm 4 ca chặn — chuỗi `32111` và `35111` ở **src EN** và ở **dist EN** (`dist/en/iconic-watches/iwc-mark-xi/`). Vá thêm thiếu `existsSync` import trong lần đầu.
- **Bằng chứng mutation (từng chuỗi, khôi phục hash từng byte) — lần đầu thực hiện trực tiếp trên tệp repo; vòng sửa 2 (TXN-20260919-46) làm lại NGOÀI cây repo chính theo yêu cầu:** dựng cây temp tương đương trong $TEMP (bản sao CAN-KIEM-CHUNG, contentRoutes, 4 tệp content VI/EN, dist iwc-mark-xi, checker và log Pagefind), chạy checker từ cây temp;

| Ca | Thao tác | Kết quả checker |
|---|---|---|
| Trên dist cũ (chưa build lại) | chạy checker | **exit 1** — bắt 2 ca dist (`32111`, `35111`) (`log-goi-truoc-build.json`) |
| Mutation A | chèn `movement: "Calibre 32111"` vào frontmatter | **exit 1** — bắt ca src `32111` (`log-mutation-a.json`) |
| Mutation B | chèn câu `calibre 35111` vào mục "The movement" | **exit 1** — bắt ca src `35111` (`log-mutation-b.json`) |
| Baseline cây temp (nguyên bản) | chạy checker từ temp | **exit 0** (`log-mutation-baseline.json`) |
| Mutation A (temp) | chèn `movement: "Calibre 32111"` vào frontmatter bản sao | **exit 1** — bắt `chan-calibre src EN` (`log-mutation-a.json`) |
| Mutation B (temp) | chèn câu `calibre 35111` vào mục "The movement" bản sao | **exit 1** — bắt `chan-calibre src EN` (`log-mutation-b.json`) |
| Khôi phục (`cmp` khớp từng byte) | chạy lại | exit 1 chỉ còn ca dist cũ (chưa build lại) — sau build: **exit 0**; **repo chính exit 0 và không bị động suốt mutation** |

- **Chạy lại cuối vòng:** `npm run check` exit 0 (378 files, 0/0/0); `npm run build` exit 0 (297 trang / sitemap 296 / 21.802 link); `kiem-h03b.mjs` exit 0; `kiem-dist.mjs` exit 0 (21/21); G05 riêng ĐẠT 11/11; **Pagefind thật 3/3 ĐẠT**; `git diff --check` sạch. Không sửa bài VI, `contentRoutes.ts`, timeline, component, package/config.

### 4.2. Số EN thực tế sau lượt này

**11/69 bài mẫu biểu tượng có bản EN** — khớp kỳ vọng sau H03-B (freak, iwc-mark-xi).

## 5. Nghi vấn còn lại

- **CAN-KIEM-CHUNG mục 57**: mâu thuẫn calibre 32111 (frontmatter) / 35111 (thân bài) trong bài VI iwc-mark-xi — chờ nguồn chính hãng. **Bản EN đã gỡ cả `32111` và `35111` (vòng sửa 1) — không còn công bố calibre mâu thuẫn; bản VI còn mâu thuẫn và chờ nguồn chính hãng để sửa đồng thời.**
- Không phát hiện nghi vấn mới khác trong hai bài VI (các năm/tham chiếu giữ nguyên theo bản VI).

## 6. Ca chưa kiểm được

- Pagefind UI gõ truy vấn trong giao diện người dùng chưa thử thủ công (bằng chứng là API thật trên preview).
- Screen reader thật chưa kiểm (giới hạn có trước).
- Trình duyệt sâu từng cặp: đã phủ mức dist (H1, rò VI, canonical/hreflang, switcher, Pagefind API) nhưng không chạy lượt trình duyệt đầy đủ mobile/desktop/sáng/tối như H03-A (khuôn component và CSS không đổi trong gói này; cặp đại diện đã kiểm ở H03-A trên cùng khuôn).

## 7. Đề nghị phát hành

**Tệp đề nghị lưu Git khi H03-B đạt (7):**
1. `src/content/mauIconic/en/freak.md`
2. `src/content/mauIconic/en/iwc-mark-xi.md`
3. `src/i18n/contentRoutes.ts`
4. `scripts/check-g05-history-journey.mjs`
5. `src/data/timeline.json`
6. `src/components/history/HistoryTimeline.astro`
7. `docs/nghiem-thu/H03-B-2026-09-19.md`
**Kèm theo (cần GPT Work xác nhận):** `CAN-KIEM-CHUNG.md` (mục 57).
**Chỉ nội bộ:** `output/h03-b-iconic-en/`.
Gói kế: H03-C (6 hồ sơ thương hiệu EN + nghiệm thu cả 13 bài). Chưa tự thực hiện.
