# BIÊN BẢN GÓI H04 — HOÀN THIỆN QUY CHUẨN HÌNH ẢNH

**Mã gói:** H04 — danh mục DHC-H14-20260919, giao dịch TXN-20260919-26
**Ngày thực hiện:** 19/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work nghiệm thu độc lập
**HEAD lúc bắt đầu:** `345ab35` = `origin/main`, nhánh `main` (đã đối chiếu `git rev-parse`)
**Giới hạn:** gói tài liệu — chưa thay hình hàng loạt trên website. Không sửa `src/`, `public/`, CSS, component, route, cấu hình, dữ liệu nội dung, ảnh, dependency, package script. Không tạo ảnh AI, không tạo route production, không mở H12. Chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi |
|---|---|
| `docs/QUY-CHUAN-HINH-ANH.md` | **Phát hành lại toàn bộ thành phiên bản 2 (bản H04)** — thay bản 19/09 với các đính chính H01 rải rác |
| `docs/nghiem-thu/H04-2026-09-19.md` | Tạo mới — biên bản này |
| `output/h04-image-standard-audit/` | Bằng chứng nội bộ (không đề nghị commit) |

Không đụng tệp nào khác; giữ nguyên toàn bộ untracked có trước (gồm `poll-h02.mjs` ở root).

## 2. Nội dung bản H04 — đối chiếu 9 yêu cầu của prompt

| # | Yêu cầu | Thực hiện ở đâu trong bản H04 |
|---|---|---|
| 1 | Trạng thái "đề xuất trình duyệt" | Header: **Trạng thái: ĐỀ XUẤT TRÌNH DUYỆT — chưa được chủ dự án phê duyệt**; hình hiện có giữ nguyên tới khi duyệt |
| 2 | Tách ba nhóm hình | Phần B: B1 sơ đồ/infographic cơ chế, B2 SVG timeline, B3 ảnh raster và ảnh AI bối cảnh |
| 3 | Token theo nhóm; đồng thau chỉ nghĩa năng lượng trong sơ đồ | B1.1 (sơ đồ mới ưu tiên `--obs-*`, tệp cũ giữ `--ig-*`, nghĩa màu đúng phạm vi sơ đồ); B2 (timeline navy/mã cứng); B3.1 (ảnh không qua token); Phần D dòng "đồng thau" |
| 4 | Ba tỷ lệ, bốn cấp nét là mặc định có ngoại lệ; nét mảnh kiểm kích thước thật | B1.2, B1.3 — ghi "mặc định, không phải cấm tuyệt đối", ngoại lệ có lý do, kiểm ở kích thước hiển thị thực |
| 5 | Ưu tiên HTML, không cấm tuyệt đối chữ SVG; ID theo instance + kiểm trùng | B1.5 (chữ), B1.6 (ID theo instance, kiểm trùng, mẫu `WatchImage` và 32 ID timeline) |
| 6 | Alt phân loại; không bắt mọi alt không rỗng | C.1 — bảng phân loại theo W3C WAI + `CONTENT-GUIDE.md` 2.5 |
| 7 | Ảnh AI chỉ bối cảnh; phân loại theo chức năng; nhãn/hồ sơ/kiểm thời đại/vị trí render/quyền | B3.2 — 7 điều; mốc `context` xét riêng từng mốc |
| 8 | Hiện trạng JPG đúng: `HistoryTimeline.astro`; thêm JPG không tự có nhãn/hai lớp | B3.3 — ba hệ quả biên tập; A3 ghi hiện trạng đo được |
| 9 | Checklist nhất quán tuyệt đối | Phần E — checklist tách theo ba nhóm, mỗi dòng dẫn về quy tắc đã nới; không còn quy tắc đối nghịch |

Thêm: Phần D là bảng "quy tắc cũ hoặc mâu thuẫn, quy tắc H04, ngoại lệ hoặc cách kiểm" với 10 dòng (mọi mệnh đề tuyệt đối của bản cũ được liệt kê và chốt lại); Phần F giữ danh sách không yêu cầu; Lịch sử phiên bản ghi v1 sang v2.

## 3. Đối chiếu hiện trạng mã nguồn (đo thật, không mô tả sai)

| Kiểm chứng | Kết quả |
|---|---|
| SVG timeline | 28 tệp; 28/28 khung `800x600`; 0 tệp dùng `var()`; 32 ID, mỗi tên đúng một lần toàn tập (0 trùng); 0 tệp JPG trong `public/images/timeline/` |
| Infographics | 13 component; 12 sơ đồ + `MechanismAnimation.astro` (icon 24x24); cả 12 sơ đồ trộn `--obs-*` và `--ig-*`; 11 tỷ lệ viewBox khác nhau |
| Ảnh AI | 1 tấm duy nhất: `public/images/history/balance-hairspring/banh-lac-day-toc-hero.jpg`; nhãn VI/EN nguyên văn tại `aiCaption` trong `BalanceHairspringChapter.astro` |
| Cơ chế JPG | Hàm `hasImage` + chọn `imageSrc` nằm trong `HistoryTimeline.astro`; `src/pages/lich-su.astro` không chứa cơ chế JPG; thẻ mốc render qua `WatchImage` |
| ID theo instance | `WatchImage.astro` dùng bộ đếm `globalThis` + hash djb2 từ G09-B |

## 4. Kiểm tra đã chạy (kết quả thật)

| Kiểm | Lệnh | Kết quả |
|---|---|---|
| Kiểm bằng chứng tổng | `node output/h04-image-standard-audit/kiem-h04.mjs` | **exit 0, TONG_LOI = 0** (log `log-kiem.json`): 7 đường dẫn ví dụ tồn tại; timeline 28 tệp / 0 lệch khung / 0 var / 32 ID / 0 trùng; 13 component có viewBox; nhãn VI/EN ảnh AI nguyên văn; cơ chế JPG đúng vị trí; liên kết nội bộ 0 hỏng; whitespace/EOF sạch |
| Kiểm lặp sau khi sửa tài liệu | Chạy lại lần cuối | exit 0 |
| `git diff --check` | `git diff --check` | Sạch, exit 0 (tệp H04 đều untracked nên whitespace/EOF do script đảm nhiệm) |

Không chạy build: không sửa website.

## 5. Điểm cần anh Vinh duyệt

1. **Duyệt (hoặc sửa) bản quy chuẩn phiên bản 2** — sau khi duyệt mới có hiệu lực áp dụng cho hình mới; đến đó mọi hình đang có giữ nguyên.
2. Hai điểm biên tập mà H04 chỉ ghi hiện trạng, chưa chốt: (a) có nâng cấp từng infographic cũ sang `--obs-*` theo từng lần sửa hay không (bản H04 đề nghị "khi nâng cấp vì lý do khác thì chuyển kèm"); (b) ngưỡng dung lượng ảnh bối cảnh 150 KB và tỷ lệ 4:3 là khuyến nghị cho tệp mới — nếu muốn thành điều kiện tối thượng thì anh chốt.
3. H12 (ảnh AI) chỉ được mở sau khi bản quy chuẩn được duyệt.

## 6. Giới hạn

- Chưa kiểm screen reader thật cho nhãn HTML ưu tiên — trong phạm vi kiểm tự động hiện hành chưa có công cụ này (giới hạn đã ghi từ các gói trước).
- Phần A là chụp hiện trạng 19/09/2026; thêm hình mới sau ngày này phải cập nhật lại số liệu trước khi trích dẫn.

## 7. Đề nghị phát hành

**Tệp tài liệu đề nghị lưu Git khi H04 đạt (2):** `docs/QUY-CHUAN-HINH-ANH.md`, `docs/nghiem-thu/H04-2026-09-19.md`.
**Chỉ dùng nội bộ, không đề nghị commit:** `output/h04-image-standard-audit/` (3 tệp: `kiem-h04.mjs`, `log-kiem.json`, `doi-chieu-quy-tac.md`).
Gói kế theo thứ tự bộ giao việc: H03-A (H01, H02, H04 đã đóng; H04 là điều kiện nền của H12 chứ không chặn H03-A). Chưa tự thực hiện.
