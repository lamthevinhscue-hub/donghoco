# BIÊN BẢN GÓI H07-A — 4 MỐC BỐI CẢNH THẾ GIỚI CHO TIMELINE

**Mã gói:** H07-A — danh mục DHC-H14-20260919 v1.0.0, giao dịch TXN-20260919-74
**Ngày thực hiện:** 20/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work nghiệm thu độc lập
**HEAD lúc bắt đầu:** `386cddf` = `origin/main`, nhánh `main`; không tracked đang sửa
**Giới hạn:** không sửa nội dung bài hiện có, `contentRoutes.ts`, template/kiểu dáng ngoài khuôn timeline, ảnh SVG/JPG/AI, CSS, dependency/lockfile, cấu hình deploy, CSP, RSS; chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi |
|---|---|
| `src/data/timeline.json` | +4 mốc bối cảnh chèn đúng vị trí thời gian: `universal-time-1884`, `great-depression-1929`, `atomic-second-1967`, `oil-shock-1973` (28 → 32) |
| `src/data/historyChapters.ts` | Cập nhật 6 `range` (liên tiếp, phủ 0–31) + thêm 1 câu dẫn/giới hạn cho C2/C3/C4/C5 (VI+EN); các câu giữ chương G05-A giữ nguyên văn |
| `src/components/history/HistoryTimeline.astro` | Dữ liệu hóa: số mốc trong pageLead (`${milestones.length}` thay "28" cứng); hàm `hashHopLe(maxIdx)` nhận biên từ số thẻ DOM thay `<= 27` cứng; mốc chưa có ảnh → không render khung ảnh (mục 5) |
| `src/pages/lich-su.astro` | Mô tả trang lấy số mốc từ dữ liệu (`${soMoc}`) |
| `src/pages/en/history.astro` | Như trên (EN) |
| `scripts/check-g02-history-timeline.mjs` | G2-1a: kỳ vọng = 28 slug nền + đúng 4 slug mới (danh sách, không số cứng); G2-7: số thẻ khớp JSON; G2-12: tính lại công thức chọn mốc trang chủ theo độ dài dữ liệu |
| `scripts/check-g05-history-journey.mjs` | G5-1/G5-2/G5-3/G5-4/G5-4b (so fixture THEO SLUG)/G5-5 (range liên tiếp phủ dữ liệu)/G5-6/G5-8b (truyền `maxIdx` = n−1)/G5-D1/G5-D2 — hết kỳ vọng cứng 28/52/19/25/0/3/27 |
| `scripts/check-h07-world-context.mjs` | Tạo mới — checker H07: W1 kho mốc, W2 trường + nguồn mốc mới, W3 giới hạn phi-nhân-quả + cấm dáng nhân quả khẳng định, W4 range chương, W5 dist (thẻ/culture/tiêu đề 2 ngôn ngữ/giới hạn/nguồn/nhãn số mốc/đích đọc tiếp) |
| `package.json` | Nối `check-h07-world-context.mjs` (không đối số) vào cuối chuỗi `check`; nối `check-g02 dist` + `check-g05 dist` + `check-h07 dist` vào cuối chuỗi `build` |
| `docs/nghiem-thu/H07-A-2026-09-20.md` | Biên bản này |
| `output/h07-world-context-audit/` | Bằng chứng nội bộ (không đề nghị commit) |

Không sửa: `contentRoutes.ts`, bài hiện có, ảnh, CSS, cấu hình; không tạo bài/route mới.

## 2. Nội dung 4 mốc mới (nguyên văn nguồn + giới hạn: `output/h07-world-context-audit/ho-so-nguon.md`)

| Slug | Năm | type/claimLevel | Nguồn | readMore |
|---|---|---|---|---|
| `universal-time-1884` | 1884 | culture/context | FHH History (mục 1884) | `/tu-dien/gmt` (đủ cặp EN) |
| `great-depression-1929` | 1929 | culture/context | Federal Reserve History | null |
| `atomic-second-1967` | 1967 | culture/context | BIPM — history of SI: second | null |
| `oil-shock-1973` | 1973 | culture/context | Federal Reserve History | null |

- Không dùng chữ "first/caused/led to/created the need for" hay dáng nhân quả; mỗi thẻ có `limit` + `limit_en` nêu rõ sự kiện cùng thời, không chứng minh nhân quả (checker W3 bắt buộc cả VI lẫn EN).
- Không chọn Apollo 1969 (trùng mốc Speedmaster nền) — tránh tăng số lượng vô ích.
- Nguồn truy được trong lượt này (WebFetch, nguyên văn trong hồ sơ): FHH History, Federal Reserve History ×2, BIPM. RMG/Britannica về hội nghị Washington 1884 không truy được (404/403) → mô tả thẻ 1884 theo đúng phạm vi nguồn FHH, giới hạn ghi rõ.

## 3. Kết quả kiểm tra (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Chuỗi kiểm hiện hành | `npm run check` | **exit 0** — astro check **396 files: 0 errors, 0 warnings, 0 hints**; H07 nguồn **4/4** |
| Build + toàn bộ checker dist | `npm run build` | **exit 0** — 306 HTML, sitemap 306 URL (không đổi vì không có route mới), 22.808 link; G02 **16/16** (G2-7: 32 thẻ khớp JSON; G2-12 theo công thức trang chủ), G05 **16/16** (G5-D1: 32 thẻ + 56 nguồn + 23 Giới hạn; G5-D2: 32 thẻ + 26/0/6 + "2013–present" + không rò VI), H07 dist **4/4**, G09 census plate **125/125/125 trên 68 trang** giữ nguyên |
| Mutation ngoài cây repo | `mutation-h07-run.sh` (cây temp `$TEMP/h07-mutation-tree`) | **7/7 ĐẠT** — baseline exit 0; `thieu-en` → G5-2; `nguon-http` → G5-4; `thieu-gioi-han` → W2; `claim-sai` → W2; `moc-nen` (sửa mốc nền) → G5-4b; khôi phục cmp 0 lệch + chạy sạch exit 0 (`log-mutation-*.json`) |
| `git diff --check` | `git diff --check` | Sạch, exit 0 |

## 4. Lượt trình duyệt trên preview (theo yêu cầu prompt — quan sát thủ công, không phải bằng chứng script)

VI desktop 1280×800 (`/lich-su/`): 32 thẻ; nhãn "32 mốc"; cả 4 mốc mới hiện đúng tiêu đề VI; bấm nút "Văn hóa" → 25/32 hiển thị (7 culture ẩn đúng); "Tất cả" → 32; anchor chương `#chuong-c4` hoạt động; Tab focus nhìn thấy trên nút lọc; nút thập niên đầu `data-jump-to="0"`.
EN desktop (`/en/history/`): 32 thẻ; "32 milestones"; chỉ-lớp-Culture (tắt 2 lớp kia) → đúng 7 thẻ; 4 tiêu đề EN + giới hạn EN của mốc mới render; switcher giữ hash hợp lệ `#milestone-31` (biên dữ liệu) và loại `#milestone-999`.
Mobile 390×844 (VI + EN): 32 thẻ; nút nav nổi hiện, panel mở, nhảy chương `#chuong-c2`, panel đóng; thẻ mốc mới không có khung ảnh rỗng (0 khung ảnh), mốc nền vẫn có ảnh.
Reduced-motion (emulate): 0 thẻ `reveal-pending`, opacity 1 ngay không cần cuộn.
Console preview: 1 lỗi duy nhất là `/_vercel/insights/script.js` 404 — hiện trạng preview có trước (G03), không thuộc gói.

## 5. Mâu thuẫn gặp phải và phương án tối thiểu đã chọn (cần GPT Work ghi nhận)

4 mốc mới chưa có ảnh minh họa. Khuôn cũ luôn render khung ảnh (svg không tồn tại → request 404 + plate fallback khi chạy). Nếu giữ khuôn đó: 4 thẻ mới render plate trang trí → census plate của checker **G09-B (125 plate / 68 trang — số cứng chặng 1)** lệch thành 133/72 → build KHÔNG ĐẠT. `scripts/check-g09-id-unique.mjs` **nằm ngoài danh sách tệp được phép của gói**.

**Phương án đã chọn (không cần tệp ngoài danh sách):** thẻ mốc không có tệp ảnh (jpg lẫn svg) thì không render khung ảnh/plate — tránh 404 và giữ nguyên census G09. Đã đối chiếu: census sau build đúng 125/125/125/68.

**Phương án còn mở (chờ quyết):** H07-B có thể bổ sung ảnh minh họa cho 4 mốc + khi đó cập nhật census G09 (hoặc dữ liệu hóa census) với ủy quyền riêng.

## 6. Chưa kiểm được

- Screen reader thật (giới hạn có trước).
- Production/deploy (chưa deploy — đúng phạm vi).
- Hành vi giữ hash khi tắt JS — giới hạn có trước từ G05-B, không đổi.

## 7. Đề nghị phát hành

**Tệp đề nghị lưu Git khi H07-A đạt (10):**
1. `src/data/timeline.json`
2. `src/data/historyChapters.ts`
3. `src/components/history/HistoryTimeline.astro`
4. `src/pages/lich-su.astro`
5. `src/pages/en/history.astro`
6. `scripts/check-g02-history-timeline.mjs`
7. `scripts/check-g05-history-journey.mjs`
8. `scripts/check-h07-world-context.mjs`
9. `package.json`
10. `docs/nghiem-thu/H07-A-2026-09-20.md`

**Chỉ nội bộ:** `output/h07-world-context-audit/`.
Gói kế: H07-B — chưa tự thực hiện.
