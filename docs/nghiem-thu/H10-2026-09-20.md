# BIÊN BẢN GÓI H10 — TỐI ƯU ẢNH THEO SỐ ĐO TRƯỚC–SAU

**Mã gói:** H10 — danh mục DHC-H14-20260919 v1.0.0, giao dịch TXN-20260919-84
**Ngày thực hiện:** 20/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work nghiệm thu độc lập
**HEAD lúc bắt đầu:** `80c47a0` = `origin/main`, nhánh `main`; không tracked đang sửa
**Giới hạn:** không mở H11/H12/H13/H14; không sửa WatchImage, timeline data, nội dung Markdown, ảnh AI, route, CSS thiết kế, CSP, Analytics, RSS, dependency; chưa stage/commit/push/deploy.

## 1. Kiểm kê trước khi sửa (8 trang mẫu, số đo thật — điều kiện: preview local, cache tắt CDP, viewport 1280×800, networkidle + cuộn kích hoạt lazy)

| Trang | Ảnh tải | Tổng bytes | Ghi chú |
|---|---|---|---|
| Trang chủ VI / EN | 1 (favicon 887B) | 887 | dải timeline dùng SVG |
| `/lich-su/` VI / EN | 6 (favicon + 5 SVG lazy) | 26.343 | sweep cuộn bắt 6/28 SVG — cùng giao thức trước/sau; byte/SVG không đổi |
| `/co-che/day-toc-banh-lac` VI | 2 | **88.343** | **hero JPG 87.456B — eager, đầu trang, hiển thị 443×249, natural 1200×675** + favicon |
| `/en/mechanisms/balance-and-hairspring` EN | 2 | **88.343** | như trên |
| `/mau-iconic/rolex-submariner` VI / EN | 1 (favicon) | 887 | **không có ảnh raster** — frontmatter không cover_image, chỉ technical plate (0 request) |

Kết luận kiểm kê: **mục tiêu tối ưu duy nhất có ý nghĩa là hero JPG 87.456B** tải trên 2 trang chương Bánh lắc-dây tóc. `og/hero-bg.jpg` (312KB) + `og/guilloche-tile.png` (172KB) là **tệp mồ côi** không trang nào tham chiếu (vết P3.1) — 0 tác động tải trang, giữ nguyên. 4 ảnh `og/*.jpg` chỉ crawler tải — không đổi OG. 28 SVG timeline là sơ đồ — quy tắc giữ nguyên.

## 2. Phạm vi thực tế

| Tệp | Thay đổi | Lý do |
|---|---|---|
| `public/images/history/balance-hairspring/banh-lac-day-toc-hero-760w.webp` | **Tạo mới** 25.964B (760×428) | Bản dẫn xuất web; JPG gốc giữ nguyên làm đường phục hồi |
| `public/images/history/balance-hairspring/banh-lac-day-toc-hero-1200w.webp` | **Tạo mới** 52.078B (1200×675) | Như trên (cho màn hình mật độ cao) |
| `src/components/history/BalanceHairspringChapter.astro` | `<img>` → `<picture>` + `<source type="image/webp" srcset 760w/1200w sizes="(min-width:1024px) 34vw, 100vw">`; `onerror` đổi sang `closest('picture')`/`[data-hero-fallback]` cho khớp cấu trúc mới | Component render trực tiếp ảnh; alt/width/height/eager/figcaption (nhãn AI) giữ nguyên |
| `IMAGE-MANIFEST.md` | Thêm mục E — H10: bản gốc/bản web/nơi render/quyền/quyết định giữ–thay | Yêu cầu gói |
| `scripts/check-h10-image-optimization.mjs` | **Tạo mới** — 10 ca (H10-1…H10-4 × VI/EN) | Checker H10 |
| `package.json` | Nối `check-h10-image-optimization.mjs dist` cuối chuỗi `build` | Nối checker |
| `docs/nghiem-thu/H10-2026-09-20.md` | Biên bản này | |
| `output/h10-image-optimization-audit/` | Bằng chứng nội bộ: `log-do-truoc-sau.json` + 4 ảnh màn hình | Không đề nghị commit |

**Lệnh tái tạo bản dẫn xuất** (sharp đi kèm Astro, không thêm dependency): `sharp(gốc).resize({width:760|1200}).webp({quality:80})`.

## 3. Chất lượng ảnh (đối chiếu giải mã)

WebP 1200w so JPG gốc 1200×675 (giải mã raw, so kênh RGB): **độ lệch trung bình 1,51/255 (0,6%), lệch lớn nhất 31** — không sai màu nhận ra được, không mất chi tiết rõ rệt. Ảnh chụp màn hình sáng/tối/mobile trong `output/h10-image-optimization-audit/`.

## 4. Kết quả kiểm tra (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Chuỗi kiểm hiện hành | `npm run check` | **exit 0** — astro check **399 files: 0 errors, 0 warnings, 0 hints** |
| Build + toàn bộ checker dist | `npm run build` | **exit 0** — 306 HTML, 22.808 link; H10 **10/10**; H09 10/10; G02/G05 16/16; **census G09 125/125/125 trên 68 trang giữ nguyên** |
| Đo SAU (cùng điều kiện đo TRƯỚC) | cùng script Playwright | trang chương VI/EN: hero = **760w.webp 25.964B** (browser chọn theo `sizes`); tổng ảnh trang **88.343 → 26.851B (−61.492B, −70,3%)**; các trang mẫu khác không đổi. `/lich-su/` VI lần SAU sweep bắt 8 SVG so 6 trước — nhiễu thời điểm lazy (cùng tệp cũ, byte/tệp không đổi), không liên quan H10 |
| `git diff --check` | `git diff --check` | Sạch, exit 0 |

## 5. Kiểm trình duyệt (thủ công theo yêu cầu prompt — giới hạn: local/preview, KHÔNG chứng minh CDN/production)

- **Điều kiện đo**: viết rõ trong `log-do-truoc-sau.json` (cache tắt, viewport cố định).
- **Hero đầu trang (eager)**: desktop sáng — webp, 443×250, tỉ lệ 16:9 giữ (`hero-vi-desktop-sang.png`); **CLS**: WebP 0,0263 vs ép-JPG 0,0295 (chênh 0,0031 — mức nền ~0,027 là font-swap/lazy có trước, H10 không cộng thêm).
- **Ảnh lazy**: SVG timeline hiện khi cuộn trên `/lich-su/`.
- **Chặn WebP (mô phỏng trình duyệt không hỗ trợ)**: gỡ `<source>` → img chọn **JPG gốc**, `complete`, hiển thị (`hero-vi-jpg-fallback.png`); naturalWidth 687 = 1200/1,7464 density-corrected đúng spec HTML (assertion theo `currentSrc` + `complete`, không theo naturalWidth thô).
- **Mobile 320 và 390**: không tràn ngang, hero hiện, browser chọn 760w (`hero-vi-mobile-390.png`).
- **Chế độ tối**: figure/viền theo token (`hero-vi-desktop-toi.png`).
- **EN**: hero webp, caption AI giữ nguyên.
- **Không URL gãy**: checker H10-4 quét mọi srcset/src 2 trang (3 URL ảnh/trang) tồn tại trong dist; check-links toàn site 22.808 link 0 hỏng.
- Console: 1 lỗi duy nhất `/_vercel/insights/script.js` 404 — hiện trạng preview (G03).

## 6. Giới hạn / điểm lưu ý

- **Số đo local, chưa chứng minh CDN/production** (đúng quy tắc gói — production tùy Vercel serve, cần kiểm sau deploy nếu muốn).
- Bài iconic hiện **không có ảnh raster** — nhóm "16 ảnh mẫu iconic" trong manifest cũ vẫn là placeholder (thuộc gói nội dung/ảnh khác, không tự thêm ảnh).
- Kịch bản chặn mạng WebP bằng CDP **không tương đương** trình duyệt không hỗ trợ WebP (picture không thử candidate khác khi lỗi mạng — trôi vào onerror như ảnh gãy, hành vi cũ với JPG gãy cũng vậy); đường phục hồi đúng nghĩa đã kiểm bằng cách gỡ `<source>`.

## 7. Đề nghị phát hành

**Tệp đề nghị lưu Git khi H10 đạt (7):**
1. `public/images/history/balance-hairspring/banh-lac-day-toc-hero-760w.webp`
2. `public/images/history/balance-hairspring/banh-lac-day-toc-hero-1200w.webp`
3. `src/components/history/BalanceHairspringChapter.astro`
4. `IMAGE-MANIFEST.md`
5. `scripts/check-h10-image-optimization.mjs`
6. `package.json`
7. `docs/nghiem-thu/H10-2026-09-20.md`

**Chỉ nội bộ:** `output/h10-image-optimization-audit/`.
Gói kế: theo thứ tự bộ giao việc — chưa tự thực hiện.
