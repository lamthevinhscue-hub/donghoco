# BIÊN BẢN GÓI H13 — TỆP `llms.txt` THỬ NGHIỆM

**Mã gói:** H13 — danh mục DHC-H14-20260919 v1.0.0, giao dịch TXN-20260919-101
**Ngày thực hiện:** 20/09/2026 (+0700)
**Trạng thái:** hoàn thành local — chờ GPT Work nghiệm thu độc lập
**HEAD lúc bắt đầu:** `ec99d7a` = `origin/main`, nhánh `main`; không tracked đang sửa
**Giới hạn:** không sửa robots.txt, sitemap, cấu hình Astro/Vercel, route, nội dung xuất bản, giao diện, dependency, lockfile; không mở H14-A/B, H07-B, H12-B; chưa stage/commit/push/deploy.

## 1. Phạm vi thực tế

| Tệp | Thay đổi | Lý do |
|---|---|---|
| `public/llms.txt` | **Tạo mới** — 20 URL, UTF-8 không BOM, cấu trúc llms.txt (tiêu đề `#`, mô tả `>`, 10 mục `##`) | Tệp mục tiêu của gói |
| `scripts/check-h13-llms.mjs` | **Tạo mới** — 9 ca (H13-1…H13-5) | Kiểm tệp sau build |
| `package.json` | Nối `check-h13-llms.mjs dist` cuối chuỗi `build` (sau check-h10) | Nối checker |
| `docs/nghiem-thu/H13-2026-09-20.md` | Biên bản này | |
| `output/h13-llms-audit/` | **Nội bộ:** kịch bản + log mutation | Không đề nghị commit |

**Không đổi:** robots.txt, sitemap, nội dung, giao diện, dependency, cấu hình.

## 2. Nội dung `llms.txt`

- Tên website + **mô tả song ngữ** (VI trong khối mở đầu, EN dưới dòng "English:").
- **10 nhóm, 20 URL** — toàn bộ HTTPS tuyệt đối trên `https://www.kienthucdonghoco.vn`, không query/fragment, không trùng: trang chủ VI/EN, lịch sử VI/EN, cơ chế VI/EN, từ điển VI/EN, hướng dẫn VI/EN, thương hiệu VI/EN, mẫu iconic VI/EN, giải phẫu VI/EN, so sánh VI/EN, RSS VI/EN. **Mỗi URL đã đối chiếu tồn tại trong `dist` sau build** (checker H13-3c).
- Không chứa `docs/`, `output/`, đường dẫn máy, khóa/token, draft (checker H13-4 quét 12 chuỗi cấm).
- **Không mâu thuẫn robots.txt**: tệp KHÔNG chứa bất kỳ chỉ thị bot nào (checker H13-5 cấm `user-agent`/`disallow`/`crawl-delay` trong nội dung). robots.txt hiện hành chặn bot huấn luyện và cho bot tìm kiếm/đọc theo yêu cầu — `llms.txt` không thay đổi hay diễn giải lại chính sách này, chỉ là danh sách khám phá nội dung công khai.
- **Không tuyên bố SEO/thứ hạng/được AI trích dẫn** — tệp chỉ có mô tả nội dung và liên kết.

## 3. Kết quả kiểm tra (kết quả thật)

| Kiểm | Lệnh/cách | Kết quả |
|---|---|---|
| Chuỗi kiểm hiện hành | `npm run check` (chạy trong build) | **exit 0** — astro check **403 files: 0 errors, 0 warnings, 0 hints** |
| Build + checker dist | `npm run build` | **exit 0** — 306 HTML, 22.808 link; H13 **9/9 ca (20 URL)**; H09 10/10; H10 10/10; **census G09 125/125/125 trên 68 trang** nguyên |
| Checker H13 riêng | `node scripts/check-h13-llms.mjs dist` | **exit 0, 9/9 ca** — nguồn UTF-8 không BOM + EOF; dist khớp nguồn từng byte; 20 URL đúng domain/không trùng/tồn tại; sạch dữ liệu nội bộ; không chỉ thị bot |
| UTF-8/EOF/whitespace | checker H13-1b + `git diff --check` | Không BOM, newline cuối có, **diff-check SẠCH** |
| Mutation ngoài cây | `mutation-h13-run.sh` (`/tmp/h13-mutation-tree`, khôi phục .goc trước MỖI ca) | **5/5 ĐẠT** — baseline exit 0; `url-404` → H13-3c; `url-ngoai` → H13-3b; `noi-bo` → H13-4; khôi phục cmp 0 + chạy sạch exit 0. Ghi chú: `[H13-2]` bắn kèm trong ca mutation là **đúng hành vi** (sửa nguồn trong khi dist thật không đổi) — chi tiết `log-mutation-tong-hop.json` |

## 4. Bảo trì (thủ công — đúng thực tế hiện tại)

- `llms.txt` là **tệp duy trì thủ công**: khi thêm/bớt/gỡ route, sửa `public/llms.txt` tương ứng rồi chạy `npm run build` — checker H13 trong chuỗi build sẽ **tự chặn URL hỏng** (H13-3c đối chiếu từng URL với dist), URL sai domain (H13-3b) và dữ liệu nội bộ (H13-4).
- Gỡ route: xóa dòng tương ứng trong `public/llms.txt`; checker không bắt việc THIẾU URL mới (không có nguồn sự thật danh mục route cho llms.txt) — phần duyệt thủ công khi đổi route nằm ở người bảo trì.
- Không tự động sinh từ sitemap (giữ gọn, chọn lọc biên tập); nếu sau này muốn tự sinh, là gói riêng.

## 5. Giới hạn

- `llms.txt` là quy ước khám phá tự nguyện (llmstxt.org) — **không tuyên bố tăng thứ hạng, không đảm bảo được AI sử dụng/trích dẫn, không thay đổi chính sách bot** (robots.txt vẫn là nơi điều khiển bot: bot huấn luyện bị chặn, bot tìm kiếm/đọc theo yêu cầu được phép).
- Số đo và mutation chạy local; chưa kiểm bot thật đọc tệp trên production.
- Không có endpoint báo cáo hay telemetry cho tệp này (đếm lượt đọc thuộc Analytics/CDN, ngoài phạm vi).

## 6. Đề nghị phát hành

**Tệp đề nghị lưu Git khi H13 đạt (4):**
1. `public/llms.txt`
2. `scripts/check-h13-llms.mjs`
3. `package.json`
4. `docs/nghiem-thu/H13-2026-09-20.md`

**Chỉ nội bộ:** `output/h13-llms-audit/`.
Gói kế: H14-A — chưa tự thực hiện.
