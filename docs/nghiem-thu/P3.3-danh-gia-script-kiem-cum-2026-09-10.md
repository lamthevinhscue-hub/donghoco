# P3.3 — Đánh giá tách phần chung script kiểm cụm (chặng A)

## Kết luận nghiệm thu GPT Work — TXN-20260910-40

ĐẠT phương án giữ nguyên, không mở chặng B. GPT Work đã tự chạy trình dựng sandbox, tự kiểm 4/4 và hồi quy 35/35 (exit 0); bản ghi từng ca có stdout/stderr riêng, sạch trước tiêm và hoàn nguyên khớp hash/tập đường dẫn. Kết quả JSON được sinh lại trong lượt nghiệm thu; log text cũ giữ vai trò lịch sử lượt GLM. Sandbox tự kiểm dùng chung vị trí được phép được chấp thuận vì chạy tuần tự, không đụng website và đã kiểm hoàn nguyên. Sandbox đã dọn sau kiểm tra.

Chỉ nghiệm thu các tình huống trong bộ ca hiện hành, không chứng nhận công cụ an toàn cho mọi đầu vào hoặc mọi rule đều được phủ. Không thay đổi script kiểm cụm thật; không mở lại reduced motion/pattern của P3.2. Các điểm đó đã được ghi giới hạn, không phải điều kiện chờ của P3.3. Giữ đường cơ sở build đã báo vì mã website không đổi; không có kiểm chứng production mới.

GPT Work đã đính chính diễn giải độ phủ 37→35 và bỏ khẳng định phổ quát không còn lỗi. Phạm vi commit: báo cáo này và 16 tệp output/p3.3-cluster-script-audit/. Không chuyển P1.5 trong lượt nghiệm thu này.

- Giao dịch: TXN-20260910-26 · TXN-20260910-38 (hoàn tất 4 điều kiện: bằng chứng từng ca, hoàn nguyên + hash từng ca, kiểm an toàn dùng chung, tự kiểm driver) · Danh mục DHC-REMAINING-20260906 v1.0.0
- Thực hiện: GLM thực thi · Ngày: 10/09/2026 · Nền: `b2a4ba0` (nhánh `main`, trùng origin/main; P3.2 đã commit)
- Quyết định GPT Work: **phương án (b) — giữ nguyên các script kiểm cụm, không mở chặng B**
- Trạng thái: **chặng A hoàn tất — hồ sơ chốt hướng giữ nguyên, chờ GPT Work nghiệm thu.** Chưa commit, chưa push; chưa sửa bất kỳ script kiểm cụm thật nào.

## 1. Kết luận chính

Quét toàn bộ 13 script kiểm cụm (4.139 dòng) tìm thấy lặp hành vi thật ở **khung R1–R4** (frontmatter EN, route pairs, required links, EN-link ban): cấu trúc khớp nhau theo từng khối (đọc tệp phạm vi, quét frontmatter, kiểm route/liên kết, cấm EN-link) ở **10/13 script**, chỉ khác hằng số dữ liệu (FILES, ROUTE_PAIRS, REQUIRED_LINKS, LINK_TARGETS, EN_SLUGS, FRONTMATTER_RULES, VALID enums); các khác biệt đã ghi ở mục 4.2–4.3 (legacy regulating, FRONTMATTER_RULES có điều kiện, thông báo per-cụm). Ước tính khung này chiếm **~90–110 dòng × 10 script ≈ 900–1.100 dòng** (phương pháp: đếm từ cấu trúc code regulating 210–300).

**Bằng chứng ủng hộ quyết định giữ nguyên (b)**: 35 ca sandbox kiểm tra các tình huống cụ thể trên 13 script, không chứng minh mọi rule đều đúng hoặc không còn lỗi. Chưa có lợi ích ròng đủ thuyết phục để nhận thêm rủi ro thay đổi cơ chế kiểm nội dung. Phương án tách chỉ là tham khảo chưa được duyệt.

## 2. Đường cơ sở

| Lệnh | Thời gian (+07 10/09) | Exit code | Kết quả |
| --- | --- | --- | --- |
| `npm run build` (chứa `npm run check`) — baseline | 14:10:37 → 14:12:35 | 0 | astro check 0 lỗi (log baseline), 286 trang, Pagefind 286 |
| `npm run build` — chốt cuối sau khi dựng sandbox/chạy hồi quy | 17:53:16 | 0 | 286 trang, Pagefind 286 |
| `npm run check:types` — chốt cuối | 18:01:34 | 0 | 0 lỗi, 0 cảnh báo, 3 hint (tệp `.cjs` gói P3.2 đã commit — ngoài phạm vi) |
| `git diff --check` | cuối vòng | 0 | sạch |

`npm run check` không chạy riêng ở baseline (build đã chứa); lúc chốt đã chạy riêng `check:types` exit 0. 13/13 script chạy sạch exit 0 trên nội dung hợp lệ — chạy lại lần cuối 18:08 (log: `log-baseline-build-va-13-script.txt`). Các mốc build/types trên là bằng chứng nền hiện hành; TXN-20260910-38 chỉ sửa hồ sơ và công cụ trong `output/p3.3-cluster-script-audit/`, **mã website (scripts/, src/) không đổi nên không build lại** — mốc build 17:53 vẫn là trạng thái hiện hành.

## 3. Bản đồ rule theo mã thực tế (không theo comment đầu tệp)

| Script | Dòng | Rule tags | Scope đọc | Đặc thù |
| --- | --- | --- | --- | --- |
| daily-care | 430 | R1, R2, R3, R4, R5, R5b, R5c | 12 bài vi/en + extra-link EN + DateSafety.astro (toàn tệp) + WaterResistance.astro (toàn tệp) + routes + 2 docs | R5b/R5c component scope; bảng quy đổi cấm; "về 6 giờ" cấm; WR_LIMIT allow |
| automatic-energy | 408 | R1, R2, R3, R4, R5, R7 | 12 bài vi/en + routes + 1 docs | R5 cấm Harwood/Perrelet/số năm; R7 khái quát cấu trúc |
| chronograph | 373 | R1–R9, R11 (10 tag) | 8 bài vi/en + routes | R6–R11: component Chronograph (marker/filter), Hamilton/calibre/9-cột cấm |
| calendar-complications | 361 | R1, R2, R3, R4, R5, R6, R6b, R8 | 6 bài vi/en + routes | FHH window perpetual (48 tháng/2100/2400) + moon (29 ngày/12 giờ/44 phút/2,8 giây); số moon khác cấm tuyệt đối |
| protection | 355 | R1, R2, R3, R4, R5, R7 | 6 bài vi/en + routes | gauss table/dải cấm; 15.000 gauss CHỈ trong cửa sổ METAS cùng dòng |
| care-cluster | 339 | không tag — 29 errors.push | 4 bài bảo dưỡng + DecisionMap 2 map + routes | frontmatter 5 trường; HTTPS ≥2 trong đó hãng; 8 liên kết; pattern cũ 4 bài |
| high-complications | 320 | R1, R2, R3, R4, R5, R6 | 8 bài vi/en + routes | tourbillon/repeater FHH windows; sonnerie/silent slide |
| movement-finishing | 329 | R2, R3, R4, R5, R6, R7, R8, R9 | 8 bài vi/en + routes | R6 cấm bảng Markdown (độc nhất); R7 claim giá/chất lượng; R8 lịch sử không nguồn; R9 relation trung tính; enum category mở rộng (hoàn thiện/chứng nhận) |
| financial-claims | 309 | R1–R5 (khung khác hẳn) | 6 bài phạm vi A + patek + 2 EN + thuongHieu/vi (readdir) | Quét KHỐI + tách câu disclaimer + URL đệm + flex() builder |
| regulating | 319 | R1, R2, R3, R4, R5, R7 | 10 bài vi/en (5 + 1 legacy EN) + routes + 2 docs | legacy link escapement; FHH windows; cấm bảng tần số |
| precision-certification | 273 | R1, R2, R3, R4, R5 | 6 bài vi/en + routes | cấm app/timegrapher/amplitude/± số/nhãn ngưỡng/chẩn đoán |
| brand-value-retention | 126 | R1, R2, R3 | readdir thuongHieu/vi — frontmatter `value_retention`/`value_retention_label` | Đọc khối frontmatter riêng; R2 thiếu label; R3 cụm tài chính trong ô |
| first-watch | 197 | không tag — 15 errors.push | pillar + DecisionMap + routes | people-first: cấm giữ giá/đầu tư/triệu-tỷ/ngân sách; title/excerpt bắt buộc |

**Cảnh báo xác nhận**: tag trùng tên giữa các script KHÔNG đồng nghĩa (R1 daily-care = frontmatter EN; R1 regulating = frontmatter EN + legacy; R1 financial = giữ giá/mất giá; R5 mỗi cụm một bộ pattern riêng).

## 4. Phân loại lặp

### 4.1 Lặp hành vi thật — ứng viên helper (có bằng chứng đối chiếu mã)

Đối chiếu nguyên văn `check-regulating-cluster.mjs` (210–300) vs `check-precision-certification-cluster.mjs` (R1–R4): **cấu trúc khớp theo từng khối** (khung đọc tệp phạm vi, quét frontmatter, kiểm route, cấm EN-link), khác hằng số dữ liệu và một số điểm biệt lập — legacy escapement chỉ có ở regulating, FRONTMATTER_RULES biến thiên theo cụm, thông báo lỗi per-cụm.

| Khối lặp | Số script có | Dòng mỗi script | Mô tả |
| --- | --- | --- | --- |
| R1 frontmatter EN | 9 + daily-care | ~35–45 | custom_slug, enum, false-flags, ≥2 HTTPS |
| R2 route pairs | 11 | ~8 | contentRoutes contains check |
| R3 required links | 11 | ~18 | links + target existsSync |
| R4 EN-link ban | 10 | ~10 | EN files, no VI-route links |
| R6 docs tồn tại | 8 | ~3 | DOC_FILES existsSync |
| **Tổng ước tính** | | | **~900–1.100 dòng** (phương pháp: đếm từ code regulating 210–300) |

### 4.2 Giống cú pháp nhưng khác phạm vi/ngữ nghĩa

- Tag `R5` giữa các cụm: cùng cơ chế quét dòng + `allow` window, pattern hoàn toàn khác.
- `VALID_CATEGORIES/DIFFICULTIES`: 2 biến thể (5 vs 8 mức) — helper phải nhận enum theo cấu hình.

### 4.3 Đặc thù nên giữ riêng

financial-claims (block-scanner + tách câu disclaimer + URL đệm + flex builder), brand-value (readdir + frontmatter value_retention), daily-care R5b/R5c (component scope), chronograph R6–R11 (component + chuyên môn), first-watch (DecisionMap + people-first).

## 5. Hồi quy trên sandbox cô lập

Cách tái lập theo trình tự: `tao-sandbox.cjs` dựng `output/p3.3-cluster-script-audit/sandbox/` qua module an toàn dùng chung `kiem-tra-an-toan.cjs` (xác minh repo nền, khóa vị trí sandbox vào thư mục được phép, từ chối đường dẫn tuyệt đối/`..`/segment cấm/symlink ra ngoài, dừng nếu manifest có bất kỳ dòng không hợp lệ, dừng nếu sandbox đã tồn tại mà không có marker `.sandbox-p33`, hash SHA-256 script bản sao = nguồn, sanity 1 script sạch) → `tu-kiem-driver.cjs` chạy 4 phép thử tự kiểm driver → `kiem-thu-hoi-quy.cjs` chạy ma trận 35 ca từ nền sạch.

Cơ chế từng ca: driver dựng **bản nền** (bảng hash SHA-256 + tập đường dẫn toàn sandbox, 355 tệp); trước mỗi ca **xác minh sạch** (hash khớp bản nền); thao tác tiêm chạy trong `try` với xác minh mục tiêu tồn tại và số vị trí thay đổi đúng kỳ vọng (append +1 lần, insert-after giữ afterText/cộng 1 lần inject, delete-text còn 0 lần, delete-file còn 0 tệp) rồi mới chạy script thật (cwd = sandbox); trong `finally` **hoàn nguyên** (xóa + dựng lại từ manifest + 13 script) và **đối chiếu hash + tập đường dẫn với bản nền**, ghi kết quả hoàn nguyên vào bản ghi ca. Hoàn nguyên lỗi hoặc lệch hash → cờ `sandboxBan` bật thật, dừng các ca sau, exit tổng ≠ 0.

Bản ghi ca lưu đầy đủ: stdout và stderr **riêng**, thời điểm bắt đầu/kết thúc ISO + thời lượng, script/fixture/action/nội dung tiêm, kỳ vọng vs thực tế, và phân tích thông báo — số dòng output khớp `expectMsg`, cụm rule tag (`[R5c]`, `R2`…), tệp+dòng mà script báo (nếu có), thông báo có thuộc fixture không; script không báo số dòng được ghi rõ là đặc tính script (ví dụ ca M01 rule `[FILE]`). Lỗi khởi chạy/timeout được ghi vào `loiVanHanh` và **không** tính là bắt được vi phạm. Kết quả máy-đọc: `kiem-thu-hoi-quy-ket-qua.json`; log chạy chốt: `log-hoi-quy-chot.txt`.

### 5.1 Kết quả (chốt 18:44 +07, 35/35 ĐẠT, exit tổng 0)

| Nhóm ca | Số ca | Kỳ vọng | Kết quả |
| --- | --- | --- | --- |
| Phiên sạch: 13 script trên nội dung hợp lệ | 13 | exit 0 | 13/13 ĐẠT |
| Vi phạm per-script (tiêm 1 câu cấm) | 13 | exit 1 + thông báo khớp | 13/13 ĐẠT |
| Ngoại lệ hợp lệ (FHH / allow manual / METAS / disclaimer) | 4 | exit 0 | 4/4 ĐẠT |
| Vi phạm cạnh ngoại lệ (dòng kế FHH; câu sau disclaimer) | 2 | exit 1 + bắt đúng cụm | 2/2 ĐẠT |
| Thiếu tệp / thiếu liên kết bắt buộc / thiếu trường frontmatter | 3 | exit 1 + phát hiện | 3/3 ĐẠT |
| **Tổng** | **35** | | **35/35 ĐẠT** |

Toàn bộ 35 ca: sạch trước tiêm 35/35, hoàn nguyên khớp hash + tập đường dẫn 35/35.

**Từ bộ 37 cũ về 35 ca hiện hành**: V13 dùng ca component WaterResistance thay ca vi phạm số vòng trong Markdown của daily-care; không còn nhóm hai ca component riêng. Ca ngoại lệ component "theo Seiko" không nằm trong bộ hiện hành. Vì vậy bộ 35 không có cùng độ phủ hành vi với bộ 37 cũ; không khẳng định bỏ ca ngoại lệ là không giảm độ phủ. GPT Work chấp thuận bộ hiện hành làm đường cơ sở giới hạn cho kết luận giữ nguyên, không làm cổng tái cấu trúc. V12 hiện xóa trường updated trong frontmatter.

Vòng 1 có 5 ca TRUỢT, cả 5 do lỗi thiết kế ca kiểm chứ không phải hành vi script: (a) driver khôi phục sandbox từng bỏ qua thư mục trong manifest → sandbox thiếu 2 bài phạm vi của care và first-watch (2 ca sạch trượt); (b) 3 ca tiêm sai cơ chế — append cuối tệp không vào được khối frontmatter (brand-value, care) và expectMsg không khớp thông báo thật (daily-care R5c). Đã sửa driver/manifest/thiết kế ca; không sửa script kiểm cụm nào.

### 5.2 13 ca vi phạm per-script

| Ca | Script | Thao tác tiêm | Kỳ vọng | Thực tế |
| --- | --- | --- | --- | --- |
| V01 | automatic-energy | append "Harwood 1923" vào len-day-tu-dong.md | exit 1 + "Harwood" | ĐẠT |
| V02 | calendar-complications | append "Mudge 1762" vào perpetual-calendar.md | exit 1 + "Mudge" | ĐẠT |
| V03 | chronograph | append "Hamilton" vào chronograph.md | exit 1 + "Hamilton" | ĐẠT |
| V04 | protection | append "4.800 gauss" vào chong-tu.md | exit 1 + "4.800 gauss" | ĐẠT |
| V05 | regulating | append "phát minh quan trọng nhất" vào bo-thoat.md | exit 1 + "quan trọng nhất" | ĐẠT |
| V06 | precision-certification | append "±15 giây/ngày" vào chronometer.md | exit 1 + "±" | ĐẠT |
| V07 | movement-finishing | append bảng Markdown vào guilloche.md | exit 1 + "bảng Markdown" | ĐẠT |
| V08 | brand-value-retention | chèn `value_retention` không nhãn vào frontmatter tissot.md | exit 1 + "value_retention_label" | ĐẠT |
| V09 | financial-claims | append "khoản đầu tư an toàn" vào microbrand-la-gi.md | exit 1 + "nhận định đầu tư" | ĐẠT |
| V10 | first-watch | append "giữ giá tốt" vào chon-dong-ho-dau-tien.md | exit 1 + "giữ giá" | ĐẠT |
| V11 | high-complications | append "Breguet… 1801" vào tourbillon.md | exit 1 + "Breguet" | ĐẠT |
| V12 | care-cluster | xóa trường `updated` khỏi frontmatter bao-duong-dong-ho.md | exit 1 + "updated" | ĐẠT |
| V13 | daily-care | append `<p>Chịu 50 m khi bơi.</p>` vào WaterResistance.astro | exit 1 + "R5c" | ĐẠT |

### 5.3 Ngoại lệ hợp lệ và vi phạm cạnh ngoại lệ

| Ca | Script | Nội dung | Kỳ vọng | Thực tế |
| --- | --- | --- | --- | --- |
| E01 | regulating | "Theo FHH, đây là mốc quan trọng nhất." | exit 0 (cửa sổ FHH qua) | ĐẠT |
| E02 | daily-care | "Không ghi số vòng… theo manual." | exit 0 (allow kèm manual) | ĐẠT |
| E03 | protection | "15.000 gauss theo chứng nhận METAS" | exit 0 (cửa sổ METAS) | ĐẠT |
| E04 | financial-claims | "không phải tư vấn đầu tư" | exit 0 (disclaimer qua) | ĐẠT |
| N01 | regulating | dòng FHH + dòng kế "quan trọng nhất" | exit 1, bắt đúng dòng cấm | ĐẠT |
| N02 | financial-claims | disclaimer + câu sau "rẻ nhất" | exit 1, bắt "rẻ nhất" | ĐẠT |

### 5.4 Thiếu tệp / liên kết / trường

| Ca | Script | Thao tác | Kỳ vọng | Thực tế |
| --- | --- | --- | --- | --- |
| M01 | regulating | xóa tuDien/vi/vph.md | exit 1 + phát hiện thiếu tệp | ĐẠT |
| M02 | daily-care | xóa liên kết `](/tu-dien/num-van)` | exit 1 + "liên kết bắt buộc" | ĐẠT |
| M03 | first-watch | xóa trường `title:` bài trụ cột | exit 1 + "title" | ĐẠT |

### 5.5 Tự kiểm driver trước khi chạy chốt (TXN-20260910-38, điều kiện 4)

`tu-kiem-driver.cjs` chạy 4 phép thử trong môi trường cô lập, không sửa script kiểm cụm thật, không đụng trạng thái cần bảo vệ (P4 đối chiếu hash sandbox chính + nội dung manifest chính trước/sau). Chạy trước chạy chốt (tự kiểm 11:43:55Z, chốt 11:44:59Z giờ UTC):

| Phép thử | Nội dung | Kết quả |
| --- | --- | --- |
| P1 sai kỳ vọng | chạy driver với `P33_CHI_CA=S01 P33_SAI_KY_VONG=S01` — kỳ vọng bị đổi +1 | S01 TRUỢT, hoàn nguyên sạch, exit driver 1 — ĐẠT |
| P2 lỗi tiêm | `P33_LOI_TIEM=V03` — fixture ép không tồn tại | bản ghi LOI-VAN-HANH + lý do lỗi tiêm, script KHÔNG chạy, `finally` hoàn nguyên sạch, exit 1 — ĐẠT |
| P3 hash phát hiện thay đổi | dựng sandbox chuẩn, sửa 1 tệp thử, đối chiếu bản nền | lệch phát hiện đúng tệp; khôi phục → khớp lại — ĐẠT |
| P4 manifest không hợp lệ | manifest thử chứa `../`, đường dẫn tuyệt đối, `..` ngầm | bị từ chối ngay khi đọc (3 dòng); sandbox chính + manifest chính nguyên vẹn (hash trước = sau) — ĐẠT |

Tổng: **4/4 ĐẠT, exit 0** (`tu-kiem-driver-ket-qua.json`, `log-tu-kiem-driver.txt`).

## 6. Đánh giá và đề xuất

**Lặp thật (ứng viên helper)**: khung R1–R4 ở mục 4.1 — cấu trúc khớp theo từng khối ở 10/13 script, chỉ khác hằng số dữ liệu (các khác biệt đã ghi ở 4.1–4.3); ước tính ~900–1.100 dòng.

**Chi phí nếu tách (chặng B)**:

- Helper chung phải tham số hóa ít nhất 7 nhóm hằng (FILES, ROUTE_PAIRS, REQUIRED_LINKS, LINK_TARGETS, EN_SLUGS, FRONTMATTER_RULES, VALID enums) và 2 biến thể enum (5 vs 8 mức); thông báo lỗi từng script khác nhau về chữ → mọi biên bản nghiệm thu hiện hành khớp chuỗi thông báo sẽ phải đối chiếu lại.
- Tag trùng tên giữa các script không đồng nghĩa (cảnh báo mục 3) → helper theo tag là bẫy; phải tách theo hành vi, đọc kỹ từng script trước khi gộp.
- Coupling chéo: sửa khung chung ảnh hưởng đồng loạt 10+ script; mỗi lần chỉnh cần chạy lại toàn bộ hồi quy cụm + `npm run check`.

**Lợi ích nếu giữ nguyên (đã chọn)**:

- Mỗi cụm tự chứa: sửa một cụm không vỡ cụm khác; các biên bản nghiệm thu cụm trước đây đã chứng minh hành vi ổn định theo từng cụm.
- Hồi quy 35 ca này trở thành đường an toàn: nếu sau này quyết định tách, chạy lại driver trước/sau để chứng minh không hồi quy hành vi.

**Đề xuất**: giữ nguyên ở mức khung R1–R4 hiện tại; nếu một ngày cần tách, bắt đầu từ R2 (route pairs, ~8 dòng × 11 script, ít biến thể) rồi R3 (required links), để R1 (frontmatter EN) làm cuối vì FRONTMATTER_RULES khác nhau tinh vi nhất. Nhân rộng chỉ khi có hồ sơ nguồn riêng trước khi chỉnh (tương tự quy tắc sơ đồ tiến hóa mẫu iconic).

## 7. Giới hạn

- **Bộ 35 ca là đường cơ sở giới hạn**: chứng minh hành vi hiện hữu của 13 script trên các tình huống đã liệt kê; **không chứng minh mọi rule của từng script được phủ**, và không đủ làm điều kiện tiên quyết để tái cấu trúc.
- Sandbox là bản sao tối thiểu theo manifest (224 dòng: 212 tệp rời + 12 thư mục copy đệ quy; 355 tệp trong sandbox kể cả marker), không phải toàn repo — kết quả giới hạn ở phạm vi tệp mà 13 script đọc.
- Driver khôi phục sandbox bằng cách xóa và dựng lại từ manifest trong `finally` trước mỗi ca tiếp theo — chi phí I/O cao hơn snapshot, đổi lại độc lập trạng thái ca trước; mỗi ca vẫn được xác minh sạch trước tiêm và đối chiếu hash sau hoàn nguyên.
- Ước tính dòng lặp (~900–1.100) là phương pháp đếm khối từ code regulating (210–300), chưa làm PoC tách thật nên không phải con số tiết kiệm cam kết.
- `npm run check:types` còn **3 hint ts(6133)** (biến không dùng) thuộc tệp `.cjs` gói P3.2 đã commit `b2a4ba0` — đây là **trạng thái đường cơ sở hiện hành** của nền, không phải "0/0/0"; ngoài phạm vi gói này nên không sửa.
- Script kiểm chạy local; không có khía cạnh production/Vercel cần kiểm trong gói này.

## 8. Git

- Nền: `b2a4ba0` (nhánh `main`), giữ nguyên suốt gói; `git diff --check` sạch; tracked working tree 0 modifications — chưa đụng scripts/, src/, package.json, astro.config.mjs.
- TXN-20260910-38 chỉ sửa tệp trong `output/p3.3-cluster-script-audit/` + biên bản này; mã website không đổi.
- Tệp gói (untracked, chờ GPT Work nghiệm thu + commit): `docs/nghiem-thu/P3.3-danh-gia-script-kiem-cum-2026-09-10.md` + `output/p3.3-cluster-script-audit/` (**16 tệp**; sandbox đã dọn sau khi chốt bằng chứng).
- Chưa commit, chưa push — chờ quyết định của GPT Work.

## 9. Bàn giao

| Tệp | Vai trò |
| --- | --- |
| `docs/nghiem-thu/P3.3-danh-gia-script-kiem-cum-2026-09-10.md` | Biên bản này |
| `output/p3.3-cluster-script-audit/README.md` | Cách tái lập + trình tự tự kiểm → chốt + vòng sửa ca |
| `output/p3.3-cluster-script-audit/kiem-tra-an-toan.cjs` | Module kiểm tra an toàn dùng chung (driver + trình dựng sandbox cùng một cơ chế) |
| `output/p3.3-cluster-script-audit/kiem-thu-hoi-quy.cjs` + `kiem-thu-hoi-quy-ket-qua.json` | Driver 35 ca + bản ghi đầy đủ từng ca (stdout/stderr riêng, thời gian, tiêm, hoàn nguyên + hash) |
| `output/p3.3-cluster-script-audit/tu-kiem-driver.cjs` + `tu-kiem-driver-ket-qua.json` + `tu-kiem-p1-ket-qua.json` + `tu-kiem-p2-ket-qua.json` | Tự kiểm driver 4 phép thử + kết quả tổng hợp + bản ghi ca của phép thử P1/P2 |
| `output/p3.3-cluster-script-audit/tao-sandbox.cjs` + `manifest-tep.txt` + `manifest-duong-dan.json` | Dựng lại sandbox cô lập (qua module an toàn) |
| `output/p3.3-cluster-script-audit/log-*.txt` (4 tệp) | Bằng chứng baseline, tự kiểm, hồi quy chốt, chốt cuối build/check |
